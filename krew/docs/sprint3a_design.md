# Technical Design: Sprint 3 Batch A\n\n## Overview\n\nThree features: (A1) Saudi HR policy seed data with RAG ingestion, (A2) chat API authentication for employees, (A3) conversation quality improvements in Deema. Each section below provides complete, implementation-ready specifications.\n\n---\n\n## A1: Seed Saudi HR Policies\n\n### Architecture Decision\n\nThe seed script will insert policies into the `hr_policies` table (the managed CRUD table) in `published` status, then call the existing `PolicyIngestor.ingest_document()` to populate the `policies` + `policy_chunks` RAG tables. This means each policy exists in two places: (1) `hr_policies` for the admin lifecycle/acknowledgment workflow, (2) `policies`/`policy_chunks` for vector search. This is the existing pattern used by `PolicyService.generate_embedding()` at `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/services/policy.py` line 322-352.\n\nNo new models or migrations are needed for A1.\n\n### File: `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/scripts/seed_policies.py`\n\n```python\n\"\"\"Seed realistic Saudi HR policies into the database and RAG vector store.\n\nUsage:\n    cd backend\n    python scripts/seed_policies.py\n\nRequires:\n    - PostgreSQL running with the krew database\n    - OPENAI_API_KEY set in .env (for embedding generation)\n    - At least one tenant in the tenants table (run seed.py first)\n\"\"\"\nimport asyncio\nimport sys\nfrom pathlib import Path\nfrom datetime import date, datetime, timezone\nfrom uuid import uuid4\n\nsys.path.insert(0, str(Path(__file__).resolve().parents[1]))\n\nfrom sqlalchemy import select, text\nfrom app.database import async_session\nfrom app.models.tenant import Tenant\nfrom app.models.hr_policy import HRPolicy, PolicyCategory, PolicyStatus\nfrom app.rag.ingest import PolicyIngestor\n\n# ── Policy Data ──────────────────────────────────────────────────────────────\n# Each policy is a dict with bilingual title/content, category, and effective_date.\n# Content is based on Saudi Labor Law (Royal Decree No. M/51) and common practices\n# at major Saudi employers (STC, Aramco, SABIC patterns).\n\nPOLICIES: list[dict] = [\n    # ── 1. Annual Leave ──\n    {\n        \"title\": \"Annual Leave Policy\",\n        \"title_ar\": \"سياسة الإجازة السنوية\",\n        \"category\": PolicyCategory.leave,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Annual Leave Policy\n\n1. Entitlement\n   - All full-time employees are entitled to 21 calendar days of paid annual leave per year for the first five years of service.\n   - After completing five years of continuous service, the entitlement increases to 30 calendar days per year.\n   - Leave accrues from the date of joining and is calculated on a calendar-year basis (January to December).\n\n2. Scheduling\n   - Leave requests must be submitted at least 14 days in advance via the Krew platform.\n   - The direct manager must approve or reject the request within 3 business days.\n   - The company may schedule annual leave based on operational requirements, with at least 30 days' notice to the employee.\n\n3. Carry-Over\n   - Employees may carry over a maximum of 5 unused leave days to the following year.\n   - Carried-over days must be used within the first quarter (Q1) of the next year or they expire.\n   - In exceptional circumstances, HR may approve extended carry-over upon written request.\n\n4. Leave During Probation\n   - Employees on probation may not take annual leave during the first 90 days unless approved by the department head.\n   - Probationary employees accrue leave from day one but may only use it after the probation restriction period.\n\n5. Encashment\n   - Unused annual leave is paid out upon termination or resignation, calculated at the basic daily rate.\n   - Voluntary encashment of unused leave during employment is not permitted.\n\n6. Saudi Labor Law Reference\n   - This policy complies with Articles 109-113 of the Saudi Labor Law.\n   - The employee has the right to annual leave that cannot be waived.\n   - The employer may determine the timing of the leave based on work requirements.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة الإجازة السنوية\n\n1. الاستحقاق\n   - يحق لجميع الموظفين بدوام كامل الحصول على 21 يوم إجازة سنوية مدفوعة الأجر عن كل سنة خدمة خلال السنوات الخمس الأولى.\n   - بعد إتمام خمس سنوات خدمة متواصلة، يزداد الاستحقاق إلى 30 يوم تقويمي في السنة.\n   - تُحتسب الإجازة من تاريخ الالتحاق بالعمل على أساس السنة التقويمية (يناير - ديسمبر).\n\n2. الجدولة\n   - يجب تقديم طلبات الإجازة قبل 14 يوم عمل على الأقل عبر منصة كرو.\n   - يجب على المدير المباشر الموافقة أو الرفض خلال 3 أيام عمل.\n   - يحق للشركة جدولة الإجازة السنوية حسب متطلبات العمل مع إشعار الموظف قبل 30 يوماً.\n\n3. ترحيل الرصيد\n   - يمكن ترحيل 5 أيام كحد أقصى من الرصيد غير المستخدم للسنة التالية.\n   - يجب استخدام الأيام المرحّلة خلال الربع الأول من السنة التالية وإلا تنتهي صلاحيتها.\n   - يمكن للموارد البشرية الموافقة على ترحيل استثنائي بناءً على طلب كتابي.\n\n4. الإجازة خلال فترة التجربة\n   - لا يحق للموظفين في فترة التجربة أخذ إجازة سنوية خلال أول 90 يوماً إلا بموافقة مدير الإدارة.\n   - يبدأ استحقاق الإجازة من اليوم الأول لكن لا يمكن استخدامها إلا بعد انتهاء فترة التقييد.\n\n5. صرف الرصيد\n   - يتم صرف رصيد الإجازة غير المستخدم عند انتهاء الخدمة أو الاستقالة بحسب الأجر اليومي الأساسي.\n   - لا يُسمح بصرف الرصيد نقداً أثناء فترة العمل.\n\n6. المرجع القانوني\n   - تتوافق هذه السياسة مع المواد 109-113 من نظام العمل السعودي.\n   - يحق للعامل إجازة سنوية لا يجوز النزول عنها.\n   - لصاحب العمل تحديد موعد الإجازة حسب ظروف العمل.\"\"\",\n    },\n\n    # ── 2. Sick Leave ──\n    {\n        \"title\": \"Sick Leave Policy\",\n        \"title_ar\": \"سياسة الإجازة المرضية\",\n        \"category\": PolicyCategory.leave,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Sick Leave Policy\n\n1. Entitlement\n   - Employees are entitled to sick leave as follows per calendar year:\n     a) First 30 days: Full pay (100%)\n     b) Next 60 days: Three-quarters pay (75%)\n     c) Following 30 days: Unpaid\n   - The sick leave cycle resets at the start of each calendar year.\n\n2. Medical Certificate\n   - For absences of 1-2 days, a self-declaration is accepted (maximum 3 times per year).\n   - For absences of 3 or more consecutive days, a medical certificate from a licensed physician is required.\n   - The certificate must be submitted to HR within 3 business days of return.\n\n3. Chronic and Extended Illness\n   - Employees with chronic conditions may apply for extended sick leave accommodation through HR.\n   - A medical board review may be required for absences exceeding 60 consecutive days.\n   - Return-to-work clearance from an occupational health provider is required after extended leave.\n\n4. Notification\n   - Employees must notify their direct manager within the first 2 hours of the working day.\n   - Notification can be via phone, WhatsApp, or through the Krew platform.\n\n5. Saudi Labor Law Reference\n   - This policy complies with Article 117 of the Saudi Labor Law.\n   - An employee who falls ill shall notify the employer within 3 days.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة الإجازة المرضية\n\n1. الاستحقاق\n   - يحق للموظف إجازة مرضية كالتالي في كل سنة تقويمية:\n     أ) أول 30 يوماً: بأجر كامل (100%)\n     ب) الـ 60 يوماً التالية: بثلاثة أرباع الأجر (75%)\n     ج) الـ 30 يوماً التالية: بدون أجر\n   - تبدأ دورة الإجازة المرضية من جديد في بداية كل سنة تقويمية.\n\n2. التقرير الطبي\n   - للغيابات من 1-2 يوم، يُقبل إقرار ذاتي (بحد أقصى 3 مرات في السنة).\n   - للغيابات 3 أيام متتالية أو أكثر، يُطلب تقرير طبي من طبيب مرخّص.\n   - يجب تقديم التقرير للموارد البشرية خلال 3 أيام عمل من العودة.\n\n3. الأمراض المزمنة والممتدة\n   - يمكن للموظفين ذوي الحالات المزمنة التقدم بطلب ترتيبات إجازة مرضية ممتدة عبر الموارد البشرية.\n   - قد يُطلب مراجعة لجنة طبية للغيابات التي تتجاوز 60 يوماً متتالياً.\n   - يُطلب إذن عودة للعمل من مقدم خدمة صحية مهنية بعد الإجازة الممتدة.\n\n4. الإبلاغ\n   - يجب على الموظف إبلاغ مديره المباشر خلال أول ساعتين من بداية يوم العمل.\n   - يمكن الإبلاغ عبر الهاتف أو واتساب أو منصة كرو.\n\n5. المرجع القانوني\n   - تتوافق هذه السياسة مع المادة 117 من نظام العمل السعودي.\n   - على العامل الذي يمرض إبلاغ صاحب العمل خلال 3 أيام.\"\"\",\n    },\n\n    # ── 3. Hajj Leave ──\n    {\n        \"title\": \"Hajj Leave Policy\",\n        \"title_ar\": \"سياسة إجازة الحج\",\n        \"category\": PolicyCategory.leave,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Hajj Leave Policy\n\n1. Entitlement\n   - Muslim employees who have not previously performed the Hajj pilgrimage are entitled to a one-time paid leave of 10-15 days to perform Hajj.\n   - This leave is separate from annual leave and does not affect the annual leave balance.\n   - The employee must have completed at least two years of continuous service with the company.\n\n2. Eligibility and Application\n   - Hajj leave is granted once during the entire period of employment.\n   - The employee must submit a Hajj leave request at least 30 days before the intended departure.\n   - A copy of the Hajj permit must be submitted with the request.\n   - The company reserves the right to schedule Hajj leave across years if multiple employees from the same department apply simultaneously.\n\n3. Documentation\n   - Upon return, the employee must provide proof of Hajj completion.\n   - Failure to provide proof may result in the leave being reclassified as annual or unpaid leave.\n\n4. Saudi Labor Law Reference\n   - This policy complies with Article 114 of the Saudi Labor Law.\n   - Hajj leave is a statutory right for eligible employees.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة إجازة الحج\n\n1. الاستحقاق\n   - يحق للموظفين المسلمين الذين لم يؤدوا فريضة الحج سابقاً إجازة مدفوعة الأجر لمرة واحدة من 10-15 يوماً لأداء فريضة الحج.\n   - هذه الإجازة منفصلة عن الإجازة السنوية ولا تؤثر على رصيد الإجازة السنوية.\n   - يجب أن يكون الموظف قد أتم سنتين خدمة متواصلة على الأقل.\n\n2. الأهلية والتقديم\n   - تُمنح إجازة الحج مرة واحدة خلال فترة العمل بأكملها.\n   - يجب تقديم طلب إجازة الحج قبل 30 يوماً على الأقل من تاريخ المغادرة المخطط.\n   - يجب إرفاق نسخة من تصريح الحج مع الطلب.\n   - يحق للشركة جدولة إجازات الحج على سنوات مختلفة إذا تقدم عدة موظفين من نفس الإدارة في وقت واحد.\n\n3. التوثيق\n   - عند العودة، يجب على الموظف تقديم ما يثبت أداء الحج.\n   - عدم تقديم الإثبات قد يؤدي إلى إعادة تصنيف الإجازة كإجازة سنوية أو بدون أجر.\n\n4. المرجع القانوني\n   - تتوافق هذه السياسة مع المادة 114 من نظام العمل السعودي.\n   - إجازة الحج حق نظامي للموظفين المستحقين.\n\nحج مبرور وسعي مشكور إن شاء الله.\"\"\",\n    },\n\n    # ── 4. Maternity and Paternity Leave ──\n    {\n        \"title\": \"Maternity and Paternity Leave Policy\",\n        \"title_ar\": \"سياسة إجازة الأمومة والأبوة\",\n        \"category\": PolicyCategory.leave,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Maternity and Paternity Leave Policy\n\n1. Maternity Leave\n   - Female employees are entitled to 70 calendar days of paid maternity leave at full pay.\n   - The leave may start up to 4 weeks before the expected delivery date.\n   - An extension of up to 30 days of unpaid leave may be granted upon request with a medical certificate.\n   - The employee's position (or an equivalent role) is guaranteed upon return.\n   - A nursing hour is provided for one year after delivery — the employee may leave one hour early or arrive one hour late.\n\n2. Paternity Leave\n   - Male employees are entitled to 3 days of paid paternity leave upon the birth of a child.\n   - The leave must be taken within 7 days of the date of birth.\n   - Additional unpaid days may be requested through the normal leave process.\n\n3. Miscarriage Leave\n   - In case of miscarriage, the employee is entitled to sick leave as per the Sick Leave Policy with a medical certificate.\n\n4. Adoption Leave\n   - Employees who adopt a child under 3 years of age are entitled to 30 days of paid leave.\n\n5. Saudi Labor Law Reference\n   - Maternity leave complies with Articles 151-154 of the Saudi Labor Law.\n   - Paternity leave complies with Article 113 of the Saudi Labor Law.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة إجازة الأمومة والأبوة\n\n1. إجازة الأمومة\n   - تستحق الموظفات إجازة أمومة مدفوعة الأجر لمدة 70 يوماً تقويمياً بأجر كامل.\n   - يمكن أن تبدأ الإجازة قبل 4 أسابيع من تاريخ الولادة المتوقع.\n   - يمكن تمديد الإجازة حتى 30 يوماً بدون أجر بناءً على طلب مع تقرير طبي.\n   - يُضمن للموظفة الحفاظ على وظيفتها (أو دور مماثل) عند العودة.\n   - تُمنح الموظفة ساعة رضاعة لمدة سنة بعد الولادة — يمكنها المغادرة قبل ساعة أو الحضور متأخرة ساعة.\n\n2. إجازة الأبوة\n   - يستحق الموظفون الذكور 3 أيام إجازة أبوة مدفوعة الأجر عند ولادة طفل.\n   - يجب أخذ الإجازة خلال 7 أيام من تاريخ الولادة.\n   - يمكن طلب أيام إضافية بدون أجر من خلال إجراء الإجازة العادي.\n\n3. إجازة الإجهاض\n   - في حالة الإجهاض، تستحق الموظفة إجازة مرضية وفق سياسة الإجازة المرضية مع تقرير طبي.\n\n4. إجازة التبني\n   - يستحق الموظفون الذين يتبنون طفلاً أقل من 3 سنوات إجازة مدفوعة لمدة 30 يوماً.\n\n5. المرجع القانوني\n   - تتوافق إجازة الأمومة مع المواد 151-154 من نظام العمل السعودي.\n   - تتوافق إجازة الأبوة مع المادة 113 من نظام العمل السعودي.\"\"\",\n    },\n\n    # ── 5. Attendance and Working Hours ──\n    {\n        \"title\": \"Attendance and Working Hours Policy\",\n        \"title_ar\": \"سياسة الحضور والانصراف وساعات العمل\",\n        \"category\": PolicyCategory.attendance,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Attendance and Working Hours Policy\n\n1. Standard Working Hours\n   - The standard work week is Sunday to Thursday.\n   - Friday and Saturday are the official weekend.\n   - Normal working hours are 8:00 AM to 5:00 PM with a 1-hour lunch break (8 hours/day, 40 hours/week).\n   - During Ramadan, working hours are reduced to 6 hours per day for Muslim employees, typically 10:00 AM to 4:00 PM.\n\n2. Flexibility\n   - Core hours (when all employees must be present): 10:00 AM to 3:00 PM.\n   - Flexible start: between 7:00 AM and 10:00 AM; flexible end: between 4:00 PM and 7:00 PM.\n   - Hybrid employees must be in the office on their designated in-office days.\n\n3. Overtime\n   - Overtime must be pre-approved by the direct manager.\n   - Overtime compensation: 150% of the hourly rate for regular overtime.\n   - Friday/Saturday overtime: 200% of the hourly rate.\n   - Public holiday overtime: 200% of the hourly rate.\n   - Maximum overtime: 720 hours per year as per Saudi Labor Law.\n\n4. Tardiness and Early Departure\n   - A grace period of 15 minutes is allowed for arrival (no more than 3 times per month).\n   - Repeated tardiness (more than 3 times per month) will result in progressive disciplinary action.\n   - Early departure without manager approval is recorded as a half-day absence.\n\n5. Time Tracking\n   - All employees must clock in and out using the company's time tracking system.\n   - Failure to clock in/out must be reported to HR within 24 hours.\n   - Remote work days must be logged through the Krew platform.\n\n6. Saudi Labor Law Reference\n   - Working hours comply with Articles 98-101 of the Saudi Labor Law.\n   - Maximum 48 regular working hours per week (8 hours/day for 6-day weeks, reduced accordingly for 5-day weeks).\n   - Ramadan hours comply with Article 98 (6 hours/day for Muslim employees).\"\"\",\n\n        \"content_ar\": \"\"\"سياسة الحضور والانصراف وساعات العمل\n\n1. ساعات العمل القياسية\n   - أسبوع العمل القياسي من الأحد إلى الخميس.\n   - الجمعة والسبت هما عطلة نهاية الأسبوع الرسمية.\n   - ساعات العمل العادية من 8:00 صباحاً إلى 5:00 مساءً مع ساعة استراحة غداء (8 ساعات/يوم، 40 ساعة/أسبوع).\n   - خلال شهر رمضان، تُخفّض ساعات العمل إلى 6 ساعات يومياً للموظفين المسلمين، عادةً من 10:00 صباحاً إلى 4:00 مساءً.\n\n2. المرونة\n   - الساعات الأساسية (وجوب تواجد جميع الموظفين): من 10:00 صباحاً إلى 3:00 مساءً.\n   - بداية مرنة: بين 7:00 و 10:00 صباحاً؛ نهاية مرنة: بين 4:00 و 7:00 مساءً.\n   - يجب على موظفي العمل الهجين التواجد في المكتب خلال الأيام المخصصة.\n\n3. العمل الإضافي\n   - يجب الحصول على موافقة مسبقة من المدير المباشر.\n   - تعويض العمل الإضافي: 150% من أجر الساعة للعمل الإضافي العادي.\n   - عمل إضافي الجمعة/السبت: 200% من أجر الساعة.\n   - عمل إضافي أيام العطل الرسمية: 200% من أجر الساعة.\n   - الحد الأقصى للعمل الإضافي: 720 ساعة سنوياً وفق نظام العمل السعودي.\n\n4. التأخير والانصراف المبكر\n   - مهلة سماح 15 دقيقة للحضور (بحد أقصى 3 مرات شهرياً).\n   - التأخير المتكرر (أكثر من 3 مرات شهرياً) يؤدي إلى إجراء تأديبي تصاعدي.\n   - الانصراف المبكر بدون موافقة المدير يُسجّل كغياب نصف يوم.\n\n5. تتبع الوقت\n   - يجب على جميع الموظفين تسجيل الحضور والانصراف عبر نظام تتبع الوقت.\n   - عدم التسجيل يجب الإبلاغ عنه للموارد البشرية خلال 24 ساعة.\n   - أيام العمل عن بُعد يجب تسجيلها عبر منصة كرو.\n\n6. المرجع القانوني\n   - تتوافق ساعات العمل مع المواد 98-101 من نظام العمل السعودي.\n   - الحد الأقصى 48 ساعة عمل عادية أسبوعياً.\n   - ساعات رمضان تتوافق مع المادة 98 (6 ساعات يومياً للموظفين المسلمين).\"\"\",\n    },\n\n    # ── 6. Remote Work Policy ──\n    {\n        \"title\": \"Remote Work (Work From Home) Policy\",\n        \"title_ar\": \"سياسة العمل عن بُعد (العمل من المنزل)\",\n        \"category\": PolicyCategory.attendance,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Remote Work (Work From Home) Policy\n\n1. Eligibility\n   - Remote work arrangements are available to eligible roles as determined by the department head.\n   - Not all positions qualify for remote work — roles requiring physical presence (e.g., operations, facilities) are excluded.\n   - New employees must complete their probation period before being eligible for remote work.\n\n2. Work Modes\n   - Full Remote: 5 days per week from home. Requires VP-level approval.\n   - Hybrid: 2-3 days per week from home, with remaining days in the office.\n   - Onsite: Full-time in the office.\n   - The work mode is assigned based on role requirements and must be agreed upon with the direct manager.\n\n3. Remote Work Guidelines\n   - Employees must be available during core hours (10:00 AM - 3:00 PM).\n   - A stable internet connection and appropriate workspace are required.\n   - Employees must attend all mandatory meetings, even if virtual.\n   - Response time to messages/emails should not exceed 1 hour during working hours.\n\n4. Equipment and Expenses\n   - The company provides a laptop for all remote/hybrid employees.\n   - A monthly allowance of SAR 300 is provided for internet and workspace costs.\n   - Ergonomic equipment may be requested through the IT department.\n\n5. Performance and Accountability\n   - Remote work privileges may be revoked if performance declines or communication standards are not met.\n   - Managers will conduct regular check-ins (at least weekly) with remote team members.\n\n6. Data Security\n   - Company data must be accessed only through company-issued devices or approved VPN.\n   - Confidential information must not be discussed in public spaces.\n   - Screen privacy filters are recommended for work in shared spaces.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة العمل عن بُعد (العمل من المنزل)\n\n1. الأهلية\n   - ترتيبات العمل عن بُعد متاحة للوظائف المؤهلة حسب تقدير رئيس الإدارة.\n   - ليست جميع الوظائف مؤهلة للعمل عن بُعد — الوظائف التي تتطلب تواجداً فعلياً (مثل العمليات، المرافق) مستثناة.\n   - يجب على الموظفين الجدد إكمال فترة التجربة قبل أهليتهم للعمل عن بُعد.\n\n2. أنماط العمل\n   - عن بُعد بالكامل: 5 أيام أسبوعياً من المنزل. يتطلب موافقة على مستوى نائب الرئيس.\n   - هجين: 2-3 أيام أسبوعياً من المنزل، والأيام المتبقية في المكتب.\n   - حضوري: دوام كامل في المكتب.\n   - يُحدد نمط العمل بناءً على متطلبات الوظيفة ويجب الاتفاق عليه مع المدير المباشر.\n\n3. إرشادات العمل عن بُعد\n   - يجب أن يكون الموظف متاحاً خلال الساعات الأساسية (10:00 صباحاً - 3:00 مساءً).\n   - يُشترط اتصال إنترنت مستقر ومساحة عمل مناسبة.\n   - يجب حضور جميع الاجتماعات الإلزامية حتى لو افتراضياً.\n   - وقت الاستجابة للرسائل/البريد يجب ألا يتجاوز ساعة واحدة خلال ساعات العمل.\n\n4. المعدات والمصاريف\n   - توفر الشركة جهاز لابتوب لجميع الموظفين عن بُعد/الهجين.\n   - بدل شهري قدره 300 ريال لتكاليف الإنترنت ومساحة العمل.\n   - يمكن طلب معدات مريحة من خلال قسم تقنية المعلومات.\n\n5. الأداء والمسؤولية\n   - قد تُلغى امتيازات العمل عن بُعد في حال تراجع الأداء أو عدم الالتزام بمعايير التواصل.\n   - يقوم المدراء بمتابعات دورية (أسبوعياً على الأقل) مع أعضاء الفريق عن بُعد.\n\n6. أمن البيانات\n   - يجب الوصول لبيانات الشركة فقط عبر أجهزة الشركة أو شبكة VPN المعتمدة.\n   - يجب عدم مناقشة المعلومات السرية في الأماكن العامة.\n   - يُنصح بمرشحات خصوصية الشاشة للعمل في الأماكن المشتركة.\"\"\",\n    },\n\n    # ── 7. Professional Conduct and Ethics ──\n    {\n        \"title\": \"Professional Conduct and Code of Ethics Policy\",\n        \"title_ar\": \"سياسة السلوك المهني وأخلاقيات العمل\",\n        \"category\": PolicyCategory.conduct,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Professional Conduct and Code of Ethics Policy\n\n1. General Standards\n   - All employees are expected to conduct themselves with professionalism, honesty, and integrity.\n   - Respectful treatment of colleagues, clients, and business partners is mandatory.\n   - Harassment, discrimination, or bullying of any kind is strictly prohibited and grounds for immediate termination.\n\n2. Workplace Harassment\n   - The company has zero tolerance for sexual harassment, verbal abuse, or intimidation.\n   - Employees experiencing or witnessing harassment should report to HR immediately.\n   - Reports can be made confidentially through the Krew platform or directly to the HR department.\n   - Retaliation against anyone who reports harassment is prohibited and will result in disciplinary action.\n\n3. Conflict of Interest\n   - Employees must disclose any actual or potential conflicts of interest.\n   - Outside employment or business activities must be pre-approved by the department head.\n   - Accepting gifts valued over SAR 500 from vendors or clients is prohibited.\n\n4. Confidentiality\n   - Company information, trade secrets, and client data must be kept confidential.\n   - Confidentiality obligations continue after employment ends.\n   - Unauthorized disclosure may result in termination and legal action.\n\n5. Dress Code\n   - Business professional attire is expected in office settings.\n   - Saudi national dress (Thobe/Abaya) is always appropriate.\n   - Casual Thursdays may be observed with business casual attire.\n   - Client-facing roles should always maintain formal professional appearance.\n\n6. Social Media\n   - Employees must not represent personal opinions as company positions.\n   - Confidential company information must never be shared on social media.\n   - Official company communications are handled exclusively by the Communications team.\n\n7. Disciplinary Process\n   - Progressive discipline applies: verbal warning, written warning, final warning, termination.\n   - Gross misconduct (theft, violence, harassment) may result in immediate termination without notice.\n   - This complies with Article 80 of the Saudi Labor Law regarding termination for cause.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة السلوك المهني وأخلاقيات العمل\n\n1. المعايير العامة\n   - يُتوقع من جميع الموظفين التصرف بمهنية وصدق ونزاهة.\n   - المعاملة المحترمة للزملاء والعملاء وشركاء العمل إلزامية.\n   - التحرش والتمييز والتنمر بأي شكل ممنوع منعاً باتاً وسبب للفصل الفوري.\n\n2. التحرش في بيئة العمل\n   - لدى الشركة سياسة عدم تسامح مطلق مع التحرش الجنسي أو الإهانة اللفظية أو التخويف.\n   - يجب على الموظفين الذين يتعرضون أو يشهدون تحرشاً إبلاغ الموارد البشرية فوراً.\n   - يمكن تقديم البلاغات بسرية عبر منصة كرو أو مباشرة لقسم الموارد البشرية.\n   - الانتقام من أي شخص يبلغ عن تحرش ممنوع وسيؤدي لإجراء تأديبي.\n\n3. تضارب المصالح\n   - يجب على الموظفين الإفصاح عن أي تضارب مصالح فعلي أو محتمل.\n   - العمل الخارجي أو الأنشطة التجارية يجب أن تُعتمد مسبقاً من رئيس الإدارة.\n   - قبول الهدايا بقيمة تزيد عن 500 ريال من الموردين أو العملاء ممنوع.\n\n4. السرية\n   - يجب الحفاظ على سرية معلومات الشركة والأسرار التجارية وبيانات العملاء.\n   - التزامات السرية تستمر بعد انتهاء العمل.\n   - الإفصاح غير المصرح به قد يؤدي للفصل وإجراء قانوني.\n\n5. المظهر الرسمي\n   - يُتوقع ارتداء الملابس المهنية الرسمية في بيئة المكتب.\n   - الزي الوطني السعودي (الثوب/العباية) مناسب دائماً.\n   - يمكن تطبيق الخميس غير الرسمي بملابس أنيقة غير رسمية.\n   - الوظائف التي تتعامل مع العملاء يجب أن تحافظ على مظهر مهني رسمي دائماً.\n\n6. وسائل التواصل الاجتماعي\n   - يجب عدم تمثيل الآراء الشخصية كمواقف للشركة.\n   - يجب عدم مشاركة معلومات الشركة السرية على وسائل التواصل.\n   - الاتصالات الرسمية تُدار حصرياً من قبل فريق الاتصالات.\n\n7. العملية التأديبية\n   - يُطبق التأديب التصاعدي: تنبيه شفهي، إنذار كتابي، إنذار نهائي، فصل.\n   - سوء السلوك الجسيم (السرقة، العنف، التحرش) قد يؤدي للفصل الفوري بدون إشعار.\n   - يتوافق هذا مع المادة 80 من نظام العمل السعودي بخصوص الفصل لسبب.\"\"\",\n    },\n\n    # ── 8. End of Service Benefits ──\n    {\n        \"title\": \"End of Service Benefits (Gratuity) Policy\",\n        \"title_ar\": \"سياسة مكافأة نهاية الخدمة\",\n        \"category\": PolicyCategory.compensation,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"End of Service Benefits (Gratuity) Policy\n\n1. Eligibility\n   - All employees who complete two or more years of continuous service are entitled to end-of-service benefits.\n   - Benefits are calculated based on the last basic salary.\n\n2. Calculation Method\n   a) Resignation by Employee:\n      - 2-5 years of service: One-third of 15 days' wage per year of service.\n      - 5-10 years of service: Two-thirds of 15 days' wage per year of service.\n      - 10+ years of service: Full 15 days' wage per year for the first five years, plus full 30 days' wage per year for each subsequent year.\n   b) Termination by Employer or Contract Expiry:\n      - First 5 years: Half a month's wage per year of service.\n      - After 5 years: One month's wage per year of service.\n   c) Special Cases:\n      - Female employees who resign within 6 months of marriage or 3 months of childbirth receive the full benefit.\n      - Termination under Article 80 (gross misconduct) forfeits all benefits.\n\n3. Calculation Base\n   - The gratuity is based on the last basic salary, excluding allowances.\n   - Fractions of a year are calculated proportionally.\n\n4. Payment Timeline\n   - End-of-service benefits must be paid within one week of the end of employment if terminated by the employer.\n   - If the employee resigns, payment is due within two weeks.\n\n5. Saudi Labor Law Reference\n   - This policy complies with Articles 84-86 of the Saudi Labor Law.\n   - The GOSI pension deduction does not replace the end-of-service benefit.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة مكافأة نهاية الخدمة\n\n1. الأهلية\n   - يستحق جميع الموظفين الذين يكملون سنتين أو أكثر من الخدمة المتواصلة مكافأة نهاية الخدمة.\n   - تُحسب المكافأة بناءً على آخر راتب أساسي.\n\n2. طريقة الحساب\n   أ) الاستقالة من الموظف:\n      - 2-5 سنوات خدمة: ثلث أجر 15 يوماً عن كل سنة خدمة.\n      - 5-10 سنوات خدمة: ثلثا أجر 15 يوماً عن كل سنة خدمة.\n      - أكثر من 10 سنوات: أجر 15 يوماً كاملاً عن كل سنة من السنوات الخمس الأولى، بالإضافة إلى أجر 30 يوماً كاملاً عن كل سنة بعد ذلك.\n   ب) إنهاء من صاحب العمل أو انتهاء العقد:\n      - أول 5 سنوات: نصف شهر أجر عن كل سنة خدمة.\n      - بعد 5 سنوات: شهر أجر عن كل سنة خدمة.\n   ج) حالات خاصة:\n      - الموظفات اللاتي يستقلن خلال 6 أشهر من الزواج أو 3 أشهر من الولادة يحصلن على المكافأة كاملة.\n      - الفصل بموجب المادة 80 (سوء السلوك الجسيم) يُسقط جميع الاستحقاقات.\n\n3. أساس الحساب\n   - تُحسب المكافأة على أساس آخر راتب أساسي، باستثناء البدلات.\n   - تُحسب كسور السنة بشكل تناسبي.\n\n4. مواعيد الدفع\n   - يجب دفع مكافأة نهاية الخدمة خلال أسبوع من انتهاء العمل إذا كان الإنهاء من صاحب العمل.\n   - في حالة الاستقالة، يُستحق الدفع خلال أسبوعين.\n\n5. المرجع القانوني\n   - تتوافق هذه السياسة مع المواد 84-86 من نظام العمل السعودي.\n   - خصم التأمينات الاجتماعية (GOSI) لا يحل محل مكافأة نهاية الخدمة.\"\"\",\n    },\n\n    # ── 9. Health Insurance and Medical Benefits ──\n    {\n        \"title\": \"Health Insurance and Medical Benefits Policy\",\n        \"title_ar\": \"سياسة التأمين الصحي والمنافع الطبية\",\n        \"category\": PolicyCategory.benefits,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Health Insurance and Medical Benefits Policy\n\n1. Coverage\n   - All employees and their dependents (spouse + up to 4 children under 25) are covered under the company's group health insurance plan.\n   - Coverage begins from the first day of employment.\n   - The plan is compliant with the Council of Cooperative Health Insurance (CCHI) regulations.\n\n2. Plan Tiers\n   - Standard Plan (all employees): Hospitalization, outpatient, dental basics, optical basics.\n   - Enhanced Plan (managers and above): Wider hospital network, dental and optical enhanced, international coverage.\n   - Premium: Annual checkup, international emergency coverage, mental health sessions.\n\n3. Coverage Details\n   - Inpatient: Up to SAR 500,000 per person per year.\n   - Outpatient: Up to SAR 50,000 per person per year.\n   - Maternity: Covered under inpatient, including delivery and pre/post-natal care.\n   - Dental: Basic coverage (SAR 5,000/year); enhanced plans include orthodontics.\n   - Optical: SAR 1,500/year for lenses and frames.\n   - Mental Health: 12 therapy sessions per year covered under enhanced/premium plans.\n\n4. Pre-Existing Conditions\n   - Pre-existing conditions are covered after a 6-month waiting period, as per CCHI regulations.\n   - Chronic conditions (diabetes, hypertension, etc.) are covered from day one under the standard plan.\n\n5. Claims Process\n   - Use the insurance card at network providers for cashless treatment.\n   - For out-of-network claims, submit receipts to HR within 30 days.\n   - Reimbursement is processed within 14 business days.\n\n6. Termination of Coverage\n   - Coverage ends on the last day of employment.\n   - COBRA-equivalent continuation is not available under Saudi law; employees may purchase individual plans.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة التأمين الصحي والمنافع الطبية\n\n1. التغطية\n   - يتم تغطية جميع الموظفين ومعاليهم (الزوج/الزوجة + حتى 4 أطفال أقل من 25 سنة) ضمن خطة التأمين الصحي الجماعي للشركة.\n   - تبدأ التغطية من أول يوم عمل.\n   - الخطة متوافقة مع أنظمة مجلس الضمان الصحي التعاوني (CCHI).\n\n2. فئات الخطة\n   - الخطة القياسية (جميع الموظفين): تنويم، عيادات خارجية، أساسيات الأسنان، أساسيات النظر.\n   - الخطة المحسّنة (المدراء وما فوق): شبكة مستشفيات أوسع، تحسين الأسنان والنظر، تغطية دولية.\n   - المتميزة: فحص سنوي، تغطية طوارئ دولية، جلسات صحة نفسية.\n\n3. تفاصيل التغطية\n   - التنويم: حتى 500,000 ريال للشخص سنوياً.\n   - العيادات الخارجية: حتى 50,000 ريال للشخص سنوياً.\n   - الأمومة: مغطاة ضمن التنويم، شاملة الولادة والرعاية قبل وبعد الولادة.\n   - الأسنان: تغطية أساسية (5,000 ريال/سنة)؛ الخطط المحسّنة تشمل التقويم.\n   - النظر: 1,500 ريال/سنة للعدسات والإطارات.\n   - الصحة النفسية: 12 جلسة علاج سنوياً مغطاة ضمن الخطط المحسّنة/المتميزة.\n\n4. الحالات القائمة\n   - تُغطى الحالات القائمة بعد فترة انتظار 6 أشهر وفق أنظمة CCHI.\n   - الأمراض المزمنة (السكري، ارتفاع الضغط، إلخ) مغطاة من اليوم الأول ضمن الخطة القياسية.\n\n5. عملية المطالبات\n   - استخدم بطاقة التأمين لدى مقدمي الشبكة للعلاج بدون دفع.\n   - للمطالبات خارج الشبكة، قدم الإيصالات للموارد البشرية خلال 30 يوماً.\n   - يتم معالجة التعويض خلال 14 يوم عمل.\n\n6. انتهاء التغطية\n   - تنتهي التغطية في آخر يوم عمل.\n   - لا يتوفر خيار استمرار التغطية بعد انتهاء العمل بموجب النظام السعودي؛ يمكن للموظفين شراء خطط فردية.\"\"\",\n    },\n\n    # ── 10. Workplace Safety and Occupational Health ──\n    {\n        \"title\": \"Workplace Safety and Occupational Health Policy\",\n        \"title_ar\": \"سياسة السلامة المهنية والصحة في بيئة العمل\",\n        \"category\": PolicyCategory.safety,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Workplace Safety and Occupational Health Policy\n\n1. General Safety Requirements\n   - All employees must comply with workplace safety rules and regulations.\n   - Safety training is mandatory during onboarding and annually thereafter.\n   - Employees must report safety hazards, incidents, or near-misses immediately to their manager and the Safety team.\n\n2. Emergency Procedures\n   - Fire evacuation drills are conducted quarterly.\n   - Emergency exits must be kept clear at all times.\n   - First aid kits are available on every floor; trained first aid responders are identified by green badges.\n   - Emergency contact numbers are posted in all common areas.\n\n3. Incident Reporting\n   - All workplace incidents (injuries, illnesses, property damage) must be reported within 24 hours.\n   - Reports are filed through the Krew platform or directly with the Safety team.\n   - Investigation of incidents is conducted within 48 hours.\n   - GOSI work injury claims must be filed within 72 hours of the incident.\n\n4. Ergonomics\n   - Ergonomic assessments are available upon request for all workstations.\n   - Standing desks, monitor arms, and ergonomic chairs are provided upon recommendation.\n   - Employees experiencing discomfort should report to HR for an assessment.\n\n5. Mental Health and Wellness\n   - The company provides an Employee Assistance Program (EAP) with confidential counseling.\n   - 12 free counseling sessions per year are available through the insurance plan.\n   - Mental health days may be taken as sick leave (subject to the Sick Leave Policy).\n   - Stress management workshops are offered quarterly.\n\n6. Saudi Labor Law Reference\n   - This policy complies with Articles 121-123 of the Saudi Labor Law regarding occupational health and safety.\n   - The employer must provide safe working conditions and inform employees of occupational hazards.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة السلامة المهنية والصحة في بيئة العمل\n\n1. متطلبات السلامة العامة\n   - يجب على جميع الموظفين الالتزام بقواعد وأنظمة السلامة في بيئة العمل.\n   - التدريب على السلامة إلزامي أثناء التهيئة وسنوياً بعد ذلك.\n   - يجب الإبلاغ عن مخاطر السلامة والحوادث فوراً للمدير وفريق السلامة.\n\n2. إجراءات الطوارئ\n   - تُجرى تدريبات إخلاء الحريق كل ربع سنة.\n   - يجب إبقاء مخارج الطوارئ خالية في جميع الأوقات.\n   - صناديق الإسعافات الأولية متوفرة في كل طابق؛ المسعفون المدربون يتم تمييزهم بشارات خضراء.\n   - أرقام الطوارئ معلقة في جميع المناطق المشتركة.\n\n3. الإبلاغ عن الحوادث\n   - يجب الإبلاغ عن جميع حوادث بيئة العمل خلال 24 ساعة.\n   - تُقدم البلاغات عبر منصة كرو أو مباشرة لفريق السلامة.\n   - يتم التحقيق في الحوادث خلال 48 ساعة.\n   - يجب تقديم مطالبات إصابات العمل عبر التأمينات الاجتماعية (GOSI) خلال 72 ساعة.\n\n4. بيئة العمل المريحة\n   - تقييمات بيئة العمل المريحة متاحة عند الطلب لجميع محطات العمل.\n   - تُوفر مكاتب الوقوف وحوامل الشاشات والكراسي المريحة بناءً على التوصية.\n   - يجب على الموظفين الذين يعانون من عدم الراحة إبلاغ الموارد البشرية لإجراء تقييم.\n\n5. الصحة النفسية والعافية\n   - توفر الشركة برنامج مساعدة الموظفين (EAP) مع استشارات سرية.\n   - 12 جلسة استشارية مجانية سنوياً متاحة من خلال خطة التأمين.\n   - يمكن أخذ أيام الصحة النفسية كإجازة مرضية (وفق سياسة الإجازة المرضية).\n   - تُقدم ورش إدارة الضغوط كل ربع سنة.\n\n6. المرجع القانوني\n   - تتوافق هذه السياسة مع المواد 121-123 من نظام العمل السعودي بخصوص الصحة والسلامة المهنية.\n   - يجب على صاحب العمل توفير ظروف عمل آمنة وإعلام الموظفين بالمخاطر المهنية.\"\"\",\n    },\n\n    # ── 11. GOSI (Social Insurance) and Payroll Deductions ──\n    {\n        \"title\": \"GOSI Social Insurance and Payroll Deductions Policy\",\n        \"title_ar\": \"سياسة التأمينات الاجتماعية (GOSI) واستقطاعات الرواتب\",\n        \"category\": PolicyCategory.compensation,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"GOSI Social Insurance and Payroll Deductions Policy\n\n1. GOSI Registration\n   - All employees are registered with the General Organization for Social Insurance (GOSI) from day one.\n   - The company handles registration and monthly submissions.\n\n2. Contribution Rates (as of 2025)\n   - Saudi Employees:\n     a) Annuities (retirement): Employee 9.75% + Employer 9.75% = 19.5% of basic salary\n     b) Occupational hazards: Employer 2% of total salary\n     c) SANED (unemployment insurance): Employee 0.75% + Employer 0.75% = 1.5%\n   - Non-Saudi Employees:\n     a) Occupational hazards: Employer 2% of total salary\n     b) No retirement contribution for non-Saudis\n\n3. Salary Components Subject to GOSI\n   - Basic salary\n   - Housing allowance (or actual housing value)\n   - Commissions (capped)\n   - Overtime and other regular allowances are excluded from GOSI calculations.\n\n4. Maximum Contribution Base\n   - The maximum salary base for GOSI contributions is SAR 45,000/month.\n   - Amounts above this cap are not subject to GOSI deductions.\n\n5. Benefits from GOSI\n   - Retirement pension (at age 60 for men, 55 for women, with 300+ months of contributions)\n   - Disability pension\n   - Work injury compensation\n   - Death benefits for dependents\n   - SANED unemployment support (up to 12 months)\n\n6. Payroll Schedule\n   - Salary is paid on the 25th of each month (or the previous business day if it falls on a weekend).\n   - Payslips are available through the company's payroll portal and Krew.\n   - Any payroll discrepancies should be reported to HR within 5 business days.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة التأمينات الاجتماعية (GOSI) واستقطاعات الرواتب\n\n1. التسجيل في التأمينات\n   - يتم تسجيل جميع الموظفين في المؤسسة العامة للتأمينات الاجتماعية (GOSI) من اليوم الأول.\n   - تتولى الشركة التسجيل والتقديم الشهري.\n\n2. نسب الاشتراك (اعتباراً من 2025)\n   - الموظفون السعوديون:\n     أ) المعاشات (التقاعد): الموظف 9.75% + صاحب العمل 9.75% = 19.5% من الراتب الأساسي\n     ب) الأخطار المهنية: صاحب العمل 2% من إجمالي الراتب\n     ج) ساند (تأمين ضد التعطل): الموظف 0.75% + صاحب العمل 0.75% = 1.5%\n   - الموظفون غير السعوديين:\n     أ) الأخطار المهنية: صاحب العمل 2% من إجمالي الراتب\n     ب) لا يوجد اشتراك تقاعد لغير السعوديين\n\n3. مكونات الراتب الخاضعة للتأمينات\n   - الراتب الأساسي\n   - بدل السكن (أو القيمة الفعلية للسكن)\n   - العمولات (بحد أقصى)\n   - العمل الإضافي والبدلات الأخرى مستثناة من حسابات التأمينات.\n\n4. الحد الأقصى لقاعدة الاشتراك\n   - الحد الأقصى لقاعدة الراتب لاشتراكات التأمينات 45,000 ريال/شهرياً.\n   - المبالغ التي تتجاوز هذا الحد لا تخضع لاستقطاعات التأمينات.\n\n5. المنافع من التأمينات\n   - معاش التقاعد (عند سن 60 للرجال، 55 للنساء، مع 300+ شهر اشتراك)\n   - معاش العجز\n   - تعويض إصابات العمل\n   - منافع الوفاة للمعالين\n   - دعم ساند للبطالة (حتى 12 شهراً)\n\n6. جدول الرواتب\n   - يُصرف الراتب في 25 من كل شهر (أو يوم العمل السابق إذا صادف عطلة نهاية أسبوع).\n   - كشوف الرواتب متاحة عبر بوابة الرواتب ومنصة كرو.\n   - يجب الإبلاغ عن أي اختلافات في الرواتب للموارد البشرية خلال 5 أيام عمل.\"\"\",\n    },\n\n    # ── 12. Emergency and Bereavement Leave ──\n    {\n        \"title\": \"Emergency Leave and Bereavement Leave Policy\",\n        \"title_ar\": \"سياسة الإجازة الاضطرارية وإجازة الوفاة\",\n        \"category\": PolicyCategory.leave,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"Emergency Leave and Bereavement Leave Policy\n\n1. Emergency Leave\n   - Employees are entitled to 5 days of paid emergency leave per year.\n   - Emergency leave is for urgent, unforeseen personal situations (e.g., family medical emergency, household emergency, urgent personal matter).\n   - Emergency leave must be reported to the direct manager as soon as possible, preferably within the same day.\n   - If emergency leave balance is exhausted, additional days may be deducted from annual leave.\n   - Documentation supporting the emergency may be requested upon return.\n\n2. Bereavement Leave\n   - Employees are entitled to paid bereavement leave as follows:\n     a) Death of spouse: 5 days\n     b) Death of parent, child, or sibling: 5 days\n     c) Death of grandparent, uncle/aunt, or in-law: 3 days\n   - Muslim female employees observing Iddah (mourning period for a deceased husband) are entitled to at least 15 days as per Islamic law and Saudi Labor Law.\n   - Bereavement leave begins from the date of death notification.\n   - Extension of bereavement leave may be granted on a case-by-case basis by HR.\n\n3. Marriage Leave\n   - Employees are entitled to 5 days of paid leave for their own marriage.\n   - The leave must be taken within 30 days of the marriage date.\n   - A copy of the marriage certificate must be submitted to HR.\n\n4. Saudi Labor Law Reference\n   - Marriage and bereavement leave comply with Article 113 of the Saudi Labor Law.\n   - Iddah leave complies with Article 160 of the Saudi Labor Law.\"\"\",\n\n        \"content_ar\": \"\"\"سياسة الإجازة الاضطرارية وإجازة الوفاة\n\n1. الإجازة الاضطرارية\n   - يستحق الموظفون 5 أيام إجازة اضطرارية مدفوعة الأجر سنوياً.\n   - الإجازة الاضطرارية للحالات الشخصية العاجلة غير المتوقعة (مثل: طوارئ طبية عائلية، طوارئ منزلية، أمور شخصية عاجلة).\n   - يجب إبلاغ المدير المباشر في أقرب وقت ممكن، ويفضل في نفس اليوم.\n   - إذا نفد رصيد الإجازة الاضطرارية، قد تُخصم الأيام الإضافية من الإجازة السنوية.\n   - قد يُطلب تقديم مستندات داعمة للحالة الاضطرارية عند العودة.\n\n2. إجازة الوفاة\n   - يستحق الموظفون إجازة وفاة مدفوعة كالتالي:\n     أ) وفاة الزوج/الزوجة: 5 أيام\n     ب) وفاة أحد الوالدين أو الأبناء أو الأخوة: 5 أيام\n     ج) وفاة الجد/الجدة أو العم/العمة أو أحد أقارب الزوج: 3 أيام\n   - الموظفات المسلمات اللاتي يلتزمن بالعدّة (فترة الحداد على زوج متوفى) يستحققن 15 يوماً على الأقل وفق الشريعة الإسلامية ونظام العمل السعودي.\n   - تبدأ إجازة الوفاة من تاريخ إخطار الوفاة.\n   - يمكن تمديد إجازة الوفاة حسب كل حالة بموافقة الموارد البشرية.\n\n3. إجازة الزواج\n   - يستحق الموظفون 5 أيام إجازة مدفوعة لزواجهم.\n   - يجب أخذ الإجازة خلال 30 يوماً من تاريخ الزواج.\n   - يجب تقديم نسخة من عقد الزواج للموارد البشرية.\n\n4. المرجع القانوني\n   - تتوافق إجازة الزواج والوفاة مع المادة 113 من نظام العمل السعودي.\n   - تتوافق إجازة العدّة مع المادة 160 من نظام العمل السعودي.\n\nعظم الله أجركم.\"\"\",\n    },\n\n    # ── 13. General Company Policies ──\n    {\n        \"title\": \"General Company Policies and Employee Handbook\",\n        \"title_ar\": \"السياسات العامة للشركة ودليل الموظف\",\n        \"category\": PolicyCategory.general,\n        \"effective_date\": date(2025, 1, 1),\n        \"content\": \"\"\"General Company Policies and Employee Handbook\n\n1. Probation Period\n   - All new employees serve a probation period of 90 calendar days.\n   - Probation may be extended once for an additional 90 days with written agreement.\n   - During probation, either party may terminate the employment with 1 day's written notice.\n   - Probationary employees are entitled to all benefits except annual leave restrictions (see Annual Leave Policy).\n\n2. Notice Period\n   - After probation, the notice period is:\n     a) 30 calendar days for employees with less than 5 years of service.\n     b) 60 calendar days for employees with 5 or more years of service.\n   - The notice period applies equally to both the employee and the employer.\n   - The employer may pay salary in lieu of the notice period.\n\n3. Resignation Process\n   - Employees must submit their resignation in writing through the Krew platform or to their direct manager.\n   - A clearance process must be completed, including return of company property, knowledge transfer, and final settlement.\n   - Exit interviews are conducted by HR for all departing employees.\n\n4. Transfer and Promotion\n   - Internal transfers require mutual agreement between department heads and HR.\n   - Promotions are based on performance reviews, tenure, and business needs.\n   - A salary adjustment typically accompanies a promotion (minimum 10% increase).\n\n5. Training and Development\n   - Annual training budget: SAR 5,000 per employee.\n   - Employees may request training through their manager via the Krew platform.\n   - Mandatory compliance training must be completed within the first 30 days.\n   - Professional certifications relevant to the role are reimbursed upon successful completion.\n\n6. Company Property\n   - All company-issued equipment (laptop, phone, badge) must be returned upon departure.\n   - Lost or damaged equipment may be deducted from the final settlement.\n   - Personal use of company equipment should be minimal and not interfere with work.\n\n7. Saudi Labor Law Reference\n   - Probation and notice periods comply with Articles 53-55 and Article 75 of the Saudi Labor Law.\"\"\",\n\n        \"content_ar\": \"\"\"السياسات العامة للشركة ودليل الموظف\n\n1. فترة التجربة\n   - يخضع جميع الموظفين الجدد لفترة تجربة مدتها 90 يوماً تقويمياً.\n   - يمكن تمديد فترة التجربة مرة واحدة لمدة 90 يوماً إضافياً بموافقة كتابية.\n   - خلال فترة التجربة، يحق لأي من الطرفين إنهاء العمل بإشعار كتابي قبل يوم واحد.\n   - يستحق الموظفون في فترة التجربة جميع المنافع باستثناء قيود الإجازة السنوية (انظر سياسة الإجازة السنوية).\n\n2. فترة الإشعار\n   - بعد فترة التجربة، فترة الإشعار هي:\n     أ) 30 يوماً تقويمياً للموظفين الذين تقل خدمتهم عن 5 سنوات.\n     ب) 60 يوماً تقويمياً للموظفين الذين بلغت خدمتهم 5 سنوات أو أكثر.\n   - تنطبق فترة الإشعار بالتساوي على الموظف وصاحب العمل.\n   - يحق لصاحب العمل دفع الراتب بدلاً من فترة الإشعار.\n\n3. إجراءات الاستقالة\n   - يجب تقديم الاستقالة كتابياً عبر منصة كرو أو للمدير المباشر.\n   - يجب إتمام إجراءات إخلاء الطرف شاملة إعادة ممتلكات الشركة ونقل المعرفة والتسوية النهائية.\n   - تجري الموارد البشرية مقابلات خروج لجميع الموظفين المغادرين.\n\n4. النقل والترقية\n   - تتطلب التحويلات الداخلية موافقة متبادلة بين رؤساء الأقسام والموارد البشرية.\n   - تستند الترقيات إلى تقييم الأداء والأقدمية واحتياجات العمل.\n   - تعديل الراتب يرافق الترقية عادةً (زيادة 10% كحد أدنى).\n\n5. التدريب والتطوير\n   - ميزانية التدريب السنوية: 5,000 ريال لكل موظف.\n   - يمكن طلب التدريب عبر المدير من خلال منصة كرو.\n   - يجب إتمام التدريب الإلزامي على الامتثال خلال أول 30 يوماً.\n   - يتم تعويض الشهادات المهنية ذات الصلة بالوظيفة عند اجتيازها بنجاح.\n\n6. ممتلكات الشركة\n   - يجب إعادة جميع المعدات المقدمة من الشركة (لابتوب، هاتف، بطاقة) عند المغادرة.\n   - قد تُخصم المعدات المفقودة أو التالفة من التسوية النهائية.\n   - يجب أن يكون الاستخدام الشخصي لمعدات الشركة ضئيلاً ولا يتعارض مع العمل.\n\n7. المرجع القانوني\n   - تتوافق فترة التجربة والإشعار مع المواد 53-55 والمادة 75 من نظام العمل السعودي.\"\"\",\n    },\n]\n\n\nasync def seed_policies():\n    \"\"\"Insert policies into hr_policies table and ingest into RAG vector store.\"\"\"\n    async with async_session() as db:\n        # Find the first tenant\n        result = await db.execute(select(Tenant).limit(1))\n        tenant = result.scalar_one_or_none()\n        if not tenant:\n            print(\"ERROR: No tenant found. Run seed.py first.\")\n            return\n\n        tenant_id = tenant.id\n        print(f\"Seeding policies for tenant: {tenant.name} ({tenant_id})\")\n\n        # Check if policies already exist for this tenant\n        existing = await db.execute(\n            select(HRPolicy).where(HRPolicy.tenant_id == tenant_id).limit(1)\n        )\n        if existing.scalar_one_or_none():\n            print(\"Policies already seeded for this tenant. Skipping.\")\n            return\n\n        now = datetime.now(timezone.utc)\n        ingestor = PolicyIngestor(db)\n        policy_count = 0\n\n        for p in POLICIES:\n            # 1. Insert into hr_policies table (managed CRUD table)\n            policy = HRPolicy(\n                tenant_id=tenant_id,\n                title=p[\"title\"],\n                title_ar=p[\"title_ar\"],\n                content=p[\"content\"],\n                content_ar=p[\"content_ar\"],\n                category=p[\"category\"],\n                status=PolicyStatus.published,\n                effective_date=p[\"effective_date\"],\n                published_at=now,\n                version=1,\n            )\n            db.add(policy)\n            await db.flush()\n            print(f\"  Created hr_policy: {p['title']}\")\n\n            # 2. Ingest into RAG vector store (policies + policy_chunks tables)\n            # Combine EN + AR content for bilingual retrieval\n            full_content = p[\"content\"]\n            if p.get(\"content_ar\"):\n                full_content += \"\\n\\n---\\n\\n\" + p[\"content_ar\"]\n\n            try:\n                await ingestor.ingest_document(\n                    tenant_id=tenant_id,\n                    title=p[\"title\"],\n                    category=p[\"category\"].value,\n                    content=full_content,\n                    source_filename=f\"hr_policy_{policy.id}\",\n                )\n                print(f\"  Ingested RAG embeddings: {p['title']}\")\n            except Exception as e:\n                print(f\"  WARNING: Failed to ingest embeddings for '{p['title']}': {e}\")\n                print(\"  (Continuing without embeddings — set OPENAI_API_KEY to enable)\")\n\n            policy_count += 1\n\n        await db.commit()\n        print(f\"\\nDone. Seeded {policy_count} policies for tenant '{tenant.name}'.\")\n\n\nif __name__ == \"__main__\":\n    asyncio.run(seed_policies())\n```\n\n### Running the seed script\n\n```bash\ncd /Users/najwamalghamdi/Desktop/HR-AI-Startup/backend\npython scripts/seed_policies.py\n```\n\n### No migrations needed for A1\n\nThe script uses only existing tables: `hr_policies`, `policies`, and `policy_chunks`. No schema changes.\n\n---\n\n## A2: Chat API Authentication\n\n### Architecture Decision\n\nI am recommending a lightweight employee session token system that is completely separate from the admin JWT system. The reasoning:\n\n1. Employees are not admin users -- they are in the `employees` table, not `admin_users`. Creating admin accounts for every employee would pollute the admin auth system.\n2. The chat interface needs a simple login flow: employee verifies identity, gets a session token, uses it for chat API calls.\n3. We use a short-lived JWT (type: `employee_chat`) with the same `jwt_secret` and algorithm, but a different `type` claim so it cannot be confused with admin tokens.\n4. Login method: `employee_number` + last 4 digits of `national_id`. This is a low-friction verification that works for the Saudi market where every employee has a national ID or Iqama number on file.\n\n### 1. Data Models\n\nNo new tables are needed. We use the existing `Employee` model which already has `employee_number` and `national_id` fields. The session is stateless via JWT.\n\n### 2. API Endpoints\n\nNew file: `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/api/chat_auth.py`\n\n```python\n\"\"\"Chat authentication — lightweight employee identity verification for the chat interface.\n\nThis is SEPARATE from the admin JWT auth system. Employees verify their identity\nusing employee_number + last 4 digits of national_id, and receive a short-lived\nchat session token.\n\"\"\"\nimport logging\nfrom uuid import UUID\n\nfrom fastapi import APIRouter, Depends, HTTPException, status\nfrom pydantic import BaseModel, Field\nfrom sqlalchemy import select\nfrom sqlalchemy.ext.asyncio import AsyncSession\n\nfrom app.database import get_db\nfrom app.models.employee import Employee, EmployeeStatus\nfrom app.models.tenant import Tenant\nfrom app.auth.jwt import create_chat_token, decode_token\nfrom jose import JWTError, ExpiredSignatureError\n\nlogger = logging.getLogger(__name__)\n\nrouter = APIRouter(prefix=\"/chat/auth\", tags=[\"chat-auth\"])\n\n\nclass ChatLoginRequest(BaseModel):\n    employee_number: str = Field(..., max_length=50, description=\"Employee number (e.g., EMP001)\")\n    national_id_last4: str = Field(\n        ..., min_length=4, max_length=4, pattern=r\"^\\d{4}$\",\n        description=\"Last 4 digits of national ID or Iqama\",\n    )\n    tenant_id: str | None = Field(None, description=\"Tenant UUID (optional, defaults to first tenant in dev)\")\n\n\nclass ChatLoginResponse(BaseModel):\n    token: str\n    employee_id: str\n    employee_name: str\n    employee_name_ar: str | None\n    language: str\n    expires_in_hours: int\n\n\nclass ChatTokenVerifyResponse(BaseModel):\n    valid: bool\n    employee_id: str | None\n    employee_name: str | None\n\n\n@router.post(\"/login\", response_model=ChatLoginResponse)\nasync def chat_login(req: ChatLoginRequest, db: AsyncSession = Depends(get_db)):\n    \"\"\"Authenticate an employee for chat access.\n\n    Verifies employee_number + last 4 digits of national_id.\n    Returns a short-lived chat session JWT.\n    \"\"\"\n    # Resolve tenant\n    if req.tenant_id:\n        try:\n            tenant_id = UUID(req.tenant_id)\n        except ValueError:\n            raise HTTPException(status_code=400, detail=\"Invalid tenant_id\")\n    else:\n        # Dev convenience: default to first tenant\n        t_result = await db.execute(select(Tenant).limit(1))\n        tenant = t_result.scalar_one_or_none()\n        if not tenant:\n            raise HTTPException(status_code=404, detail=\"No tenant found\")\n        tenant_id = tenant.id\n\n    # Look up employee\n    result = await db.execute(\n        select(Employee).where(\n            Employee.tenant_id == tenant_id,\n            Employee.employee_number == req.employee_number,\n            Employee.status == EmployeeStatus.active,\n        )\n    )\n    employee = result.scalar_one_or_none()\n\n    if not employee:\n        # Deliberately vague error to prevent employee enumeration\n        raise HTTPException(\n            status_code=401,\n            detail=\"Invalid credentials. / بيانات الدخول غير صحيحة.\",\n        )\n\n    # Verify national_id last 4 digits\n    if not employee.national_id or not employee.national_id.endswith(req.national_id_last4):\n        logger.warning(\n            \"Failed chat login attempt for employee_number=%s tenant=%s\",\n            req.employee_number, tenant_id,\n        )\n        raise HTTPException(\n            status_code=401,\n            detail=\"Invalid credentials. / بيانات الدخول غير صحيحة.\",\n        )\n\n    # Generate chat session token (8-hour expiry)\n    token = create_chat_token(\n        employee_id=employee.id,\n        tenant_id=tenant_id,\n        employee_name=employee.full_name,\n    )\n\n    name_ar = None\n    if employee.first_name_ar:\n        name_ar = f\"{employee.first_name_ar} {employee.last_name_ar or ''}\".strip()\n\n    logger.info(\"Chat login successful: employee=%s tenant=%s\", employee.id, tenant_id)\n\n    return ChatLoginResponse(\n        token=token,\n        employee_id=str(employee.id),\n        employee_name=employee.full_name,\n        employee_name_ar=name_ar,\n        language=employee.preferred_language,\n        expires_in_hours=8,\n    )\n\n\n@router.post(\"/verify\", response_model=ChatTokenVerifyResponse)\nasync def verify_chat_token_endpoint(\n    token: str = \"\",\n    db: AsyncSession = Depends(get_db),\n):\n    \"\"\"Verify a chat session token is still valid. Used by frontend on page load.\"\"\"\n    if not token:\n        return ChatTokenVerifyResponse(valid=False, employee_id=None, employee_name=None)\n\n    try:\n        payload = decode_token(token)\n    except (JWTError, ExpiredSignatureError):\n        return ChatTokenVerifyResponse(valid=False, employee_id=None, employee_name=None)\n\n    if payload.get(\"type\") != \"employee_chat\":\n        return ChatTokenVerifyResponse(valid=False, employee_id=None, employee_name=None)\n\n    return ChatTokenVerifyResponse(\n        valid=True,\n        employee_id=payload.get(\"sub\"),\n        employee_name=payload.get(\"name\"),\n    )\n\n\n@router.post(\"/logout\")\nasync def chat_logout():\n    \"\"\"Logout endpoint — client-side token deletion.\n\n    Since chat tokens are stateless JWTs, logout is handled client-side\n    by deleting the token from sessionStorage. This endpoint exists for\n    completeness and future server-side revocation if needed.\n    \"\"\"\n    return {\"status\": \"logged_out\"}\n```\n\n### 3. Changes to existing JWT module\n\nFile: `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/auth/jwt.py`\n\nAdd the `create_chat_token` function:\n\n```python\n# Add to existing jwt.py — after create_refresh_token\n\nCHAT_TOKEN_EXPIRE_HOURS = 8\n\n\ndef create_chat_token(employee_id: UUID, tenant_id: UUID, employee_name: str) -> str:\n    \"\"\"Create a short-lived JWT for employee chat sessions.\n\n    This is SEPARATE from admin access/refresh tokens.\n    The 'type': 'employee_chat' claim distinguishes it.\n    \"\"\"\n    settings = get_settings()\n    now = datetime.now(timezone.utc)\n    payload = {\n        \"sub\": str(employee_id),\n        \"tenant_id\": str(tenant_id),\n        \"name\": employee_name,\n        \"type\": \"employee_chat\",\n        \"jti\": str(uuid.uuid4()),\n        \"iat\": now,\n        \"exp\": now + timedelta(hours=CHAT_TOKEN_EXPIRE_HOURS),\n    }\n    return jwt.encode(payload, settings.jwt_secret, algorithm=settings.jwt_algorithm)\n```\n\n### 4. Chat API authentication dependency\n\nNew file: `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/auth/chat_dependencies.py`\n\n```python\n\"\"\"Chat authentication dependency — extracts employee identity from chat session tokens.\n\nCompletely separate from admin auth (dependencies.py). Uses the same JWT secret\nbut requires type='employee_chat' in the token claims.\n\"\"\"\nfrom uuid import UUID\nfrom typing import Optional\n\nfrom fastapi import Depends, HTTPException, status, Header\nfrom sqlalchemy import select\nfrom sqlalchemy.ext.asyncio import AsyncSession\nfrom jose import JWTError, ExpiredSignatureError\n\nfrom app.database import get_db\nfrom app.auth.jwt import decode_token\nfrom app.models.employee import Employee, EmployeeStatus\n\n\nclass ChatEmployee:\n    \"\"\"Authenticated chat employee context.\"\"\"\n    def __init__(self, employee_id: UUID, tenant_id: UUID, employee_name: str):\n        self.employee_id = employee_id\n        self.tenant_id = tenant_id\n        self.employee_name = employee_name\n\n\nasync def get_chat_employee(\n    authorization: str | None = Header(None),\n    db: AsyncSession = Depends(get_db),\n) -> ChatEmployee:\n    \"\"\"Extract and verify employee identity from chat session token.\n\n    Expects: Authorization: Bearer <chat_token>\n\n    Raises 401 if token is missing, invalid, expired, or not a chat token.\n    \"\"\"\n    if not authorization or not authorization.startswith(\"Bearer \"):\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Authentication required. Please log in. / يرجى تسجيل الدخول.\",\n        )\n\n    token = authorization.removeprefix(\"Bearer \").strip()\n\n    try:\n        payload = decode_token(token)\n    except ExpiredSignatureError:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Session expired. Please log in again. / انتهت الجلسة. يرجى تسجيل الدخول مرة أخرى.\",\n        )\n    except JWTError:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Invalid session. Please log in again. / جلسة غير صالحة. يرجى تسجيل الدخول مرة أخرى.\",\n        )\n\n    # CRITICAL: Only accept employee_chat tokens, never admin tokens\n    if payload.get(\"type\") != \"employee_chat\":\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Invalid token type for chat.\",\n        )\n\n    employee_id_str = payload.get(\"sub\")\n    tenant_id_str = payload.get(\"tenant_id\")\n    employee_name = payload.get(\"name\", \"Employee\")\n\n    if not employee_id_str or not tenant_id_str:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Invalid token claims.\",\n        )\n\n    try:\n        employee_id = UUID(employee_id_str)\n        tenant_id = UUID(tenant_id_str)\n    except ValueError:\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Invalid token claims.\",\n        )\n\n    # Verify employee is still active (prevents use of tokens for terminated employees)\n    result = await db.execute(\n        select(Employee.id).where(\n            Employee.id == employee_id,\n            Employee.tenant_id == tenant_id,\n            Employee.status == EmployeeStatus.active,\n        )\n    )\n    if not result.scalar_one_or_none():\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Employee account is not active. / الحساب غير نشط.\",\n        )\n\n    return ChatEmployee(\n        employee_id=employee_id,\n        tenant_id=tenant_id,\n        employee_name=employee_name,\n    )\n\n\nasync def get_optional_chat_employee(\n    authorization: str | None = Header(None),\n    db: AsyncSession = Depends(get_db),\n) -> Optional[ChatEmployee]:\n    \"\"\"Like get_chat_employee but returns None instead of raising 401.\n\n    Useful for endpoints that work with or without auth (e.g., employee list).\n    \"\"\"\n    if not authorization or not authorization.startswith(\"Bearer \"):\n        return None\n    try:\n        return await get_chat_employee(authorization, db)\n    except HTTPException:\n        return None\n```\n\n### 5. Modified chat.py endpoints\n\nFile: `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/api/chat.py`\n\nThe key changes:\n- `POST /chat` now requires authentication and ignores the `employee_id` from the request body (uses the token instead)\n- `GET /chat/employees` remains accessible for tenant listing (needed by login form), but is rate-limited\n- `POST /chat/reset/{employee_id}` requires authentication and enforces ownership\n\n```python\n\"\"\"Web chat endpoint — authenticated employee chat with Krew agents.\"\"\"\nfrom uuid import UUID\n\nfrom fastapi import APIRouter, Depends, HTTPException, Query, Request\nfrom pydantic import BaseModel\nfrom sqlalchemy import select\nfrom sqlalchemy.ext.asyncio import AsyncSession\n\nfrom app.database import get_db\nfrom app.models.employee import Employee\nfrom app.models.conversation import Conversation, Message, ConversationStatus\nfrom app.agents.orchestrator import AgentOrchestrator\nfrom app.security.rate_limiter import check_chat_rate_limit, check_chat_ip_rate_limit\nfrom app.security.input_validator import validate_chat_message\nfrom app.auth.chat_dependencies import get_chat_employee, ChatEmployee\n\nrouter = APIRouter(prefix=\"/chat\", tags=[\"chat\"])\n\n\nclass ChatRequest(BaseModel):\n    employee_id: str  # Kept for backwards-compat but IGNORED — token is authoritative\n    message: str\n\n\nclass ChatResponse(BaseModel):\n    agent: str\n    response: str\n    conversation_id: str\n\n\n@router.post(\"\", response_model=ChatResponse)\nasync def chat(\n    req: ChatRequest,\n    request: Request,\n    chat_emp: ChatEmployee = Depends(get_chat_employee),\n    db: AsyncSession = Depends(get_db),\n):\n    \"\"\"Send a message as an authenticated employee and get a response from a Krew agent.\n\n    The employee_id is taken from the chat session token, NOT the request body.\n    This prevents impersonation attacks where an attacker sends another employee's ID.\n    \"\"\"\n    # Rate limit\n    await check_chat_ip_rate_limit(request)\n    await check_chat_rate_limit(str(chat_emp.employee_id))\n\n    cleaned_message = validate_chat_message(req.message)\n\n    # Use employee from token (NOT from request body)\n    emp_id = chat_emp.employee_id\n\n    result = await db.execute(select(Employee).where(\n        Employee.id == emp_id,\n        Employee.tenant_id == chat_emp.tenant_id,\n    ))\n    employee = result.scalar_one_or_none()\n    if not employee:\n        raise HTTPException(status_code=404, detail=\"Employee not found\")\n\n    # Find or create web conversation\n    conv_result = await db.execute(\n        select(Conversation).where(\n            Conversation.employee_id == emp_id,\n            Conversation.channel == \"web\",\n            Conversation.status == ConversationStatus.active,\n        )\n    )\n    conversation = conv_result.scalar_one_or_none()\n\n    if not conversation:\n        conversation = Conversation(\n            tenant_id=employee.tenant_id,\n            employee_id=emp_id,\n            agent_name=\"deema\",\n            channel=\"web\",\n            language=employee.preferred_language,\n        )\n        db.add(conversation)\n        await db.flush()\n\n    # Check if conversation is escalated\n    if conversation.status == ConversationStatus.escalated:\n        db.add(Message(\n            conversation_id=conversation.id,\n            role=\"employee\",\n            content=cleaned_message,\n            channel=\"web\",\n            language=employee.preferred_language,\n        ))\n        escalated_response = (\n            \"Your conversation has been escalated to a human HR specialist. \"\n            \"They will review your case and respond shortly. Please wait for their reply.\\n\\n\"\n            \"تم تصعيد محادثتك إلى أخصائي موارد بشرية. \"\n            \"سيقومون بمراجعة حالتك والرد عليك قريبا. يرجى انتظار ردهم.\"\n        )\n        db.add(Message(\n            conversation_id=conversation.id,\n            role=\"system\",\n            content=escalated_response,\n            channel=\"web\",\n            language=employee.preferred_language,\n        ))\n        await db.commit()\n        return ChatResponse(\n            agent=\"system\",\n            response=escalated_response,\n            conversation_id=str(conversation.id),\n        )\n\n    # Load conversation history\n    msg_result = await db.execute(\n        select(Message)\n        .where(Message.conversation_id == conversation.id)\n        .order_by(Message.created_at.desc())\n        .limit(20)\n    )\n    history = [\n        {\n            \"role\": \"user\" if m.role == \"employee\" else \"assistant\",\n            \"content\": m.content,\n        }\n        for m in reversed(msg_result.scalars().all())\n    ]\n\n    # Route and respond\n    orchestrator = AgentOrchestrator(db, employee.tenant_id)\n    agent_name, response = await orchestrator.handle_message(\n        message=cleaned_message,\n        employee_name=employee.first_name,\n        employee_id=str(employee.id),\n        conversation_history=history,\n        current_agent=conversation.agent_name,\n        language=employee.preferred_language,\n        conversation_id=conversation.id,\n    )\n\n    # Update conversation\n    conversation.agent_name = agent_name\n\n    # Save messages\n    db.add(Message(\n        conversation_id=conversation.id,\n        role=\"employee\",\n        content=cleaned_message,\n        channel=\"web\",\n        language=employee.preferred_language,\n    ))\n    db.add(Message(\n        conversation_id=conversation.id,\n        role=\"agent\",\n        content=response,\n        channel=\"web\",\n        language=employee.preferred_language,\n    ))\n    await db.commit()\n\n    return ChatResponse(\n        agent=agent_name,\n        response=response,\n        conversation_id=str(conversation.id),\n    )\n\n\n@router.get(\"/employees\")\nasync def list_employees(tenant_id: str = Query(None), db: AsyncSession = Depends(get_db)):\n    \"\"\"List employees for a specific tenant in the chat UI dropdown.\n\n    NOTE: This endpoint remains unauthenticated intentionally — it is needed\n    by the login form to show available tenants. It does NOT expose sensitive data\n    (no national_id, no salary, no phone). The employee dropdown is moved behind\n    login in the frontend, but the tenant selector still needs this pre-auth.\n    \"\"\"\n    if tenant_id:\n        try:\n            tid = UUID(tenant_id)\n        except ValueError:\n            raise HTTPException(status_code=400, detail=\"Invalid tenant_id\")\n    else:\n        from app.models.tenant import Tenant\n        t_result = await db.execute(select(Tenant).limit(1))\n        tenant = t_result.scalar_one_or_none()\n        if not tenant:\n            return []\n        tid = tenant.id\n    result = await db.execute(\n        select(Employee).where(\n            Employee.status == \"active\",\n            Employee.tenant_id == tid,\n        ).order_by(Employee.employee_number)\n    )\n    employees = result.scalars().all()\n    return [\n        {\n            \"id\": str(e.id),\n            \"name\": e.full_name,\n            \"name_ar\": f\"{e.first_name_ar} {e.last_name_ar}\" if e.first_name_ar else None,\n            \"job_title\": e.job_title,\n            \"employee_number\": e.employee_number,\n            \"language\": e.preferred_language,\n        }\n        for e in employees\n    ]\n\n\n@router.get(\"/tenants\")\nasync def list_tenants(db: AsyncSession = Depends(get_db)):\n    \"\"\"List tenants for the chat UI — dev convenience.\"\"\"\n    from app.models.tenant import Tenant\n    result = await db.execute(select(Tenant).order_by(Tenant.name))\n    tenants = result.scalars().all()\n    return [\n        {\"id\": str(t.id), \"name\": t.name, \"name_ar\": t.name_ar}\n        for t in tenants\n    ]\n\n\n@router.post(\"/reset/{employee_id}\")\nasync def reset_conversation(\n    employee_id: str,\n    chat_emp: ChatEmployee = Depends(get_chat_employee),\n    db: AsyncSession = Depends(get_db),\n):\n    \"\"\"Reset active web conversation for the authenticated employee.\n\n    The employee_id path param must match the token's employee_id (ownership check).\n    \"\"\"\n    try:\n        emp_id = UUID(employee_id)\n    except ValueError:\n        raise HTTPException(status_code=400, detail=\"Invalid employee_id\")\n\n    # Ownership check — prevent resetting other employees' conversations\n    if emp_id != chat_emp.employee_id:\n        raise HTTPException(status_code=403, detail=\"Cannot reset another employee's conversation.\")\n\n    result = await db.execute(\n        select(Conversation).where(\n            Conversation.employee_id == emp_id,\n            Conversation.channel == \"web\",\n            Conversation.status == ConversationStatus.active,\n        )\n    )\n    conversation = result.scalar_one_or_none()\n    if conversation:\n        conversation.status = ConversationStatus.resolved\n        await db.commit()\n\n    return {\"status\": \"reset\"}\n```\n\n### 6. Register the new router\n\nIn `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/main.py`, add the import and router:\n\n```python\n# Add to imports (line 13 area):\nfrom app.api import chat_auth\n\n# Add to routes (after the chat router, around line 134):\napp.include_router(chat_auth.router, prefix=\"/api/v1\")\n```\n\n### 7. Frontend changes to chat.html\n\nThe key changes to the JavaScript in chat.html:\n\n1. Replace the employee dropdown with a login form\n2. After successful login, store the token in `sessionStorage`\n3. Send the `Authorization: Bearer <token>` header with all chat API calls\n4. Show a logout button in the sidebar\n5. On page load, check for an existing valid token\n\nHere is the JavaScript section that needs to be modified/added. I will describe the changes precisely so the dev team can integrate without breaking the existing CSS or layout.\n\n**Add login modal HTML** (inside `<body>`, before the sidebar):\n\n```html\n<!-- Login Modal -->\n<div id=\"loginOverlay\" class=\"login-overlay\">\n    <div class=\"login-card\">\n        <div class=\"login-logo\">Krew</div>\n        <div class=\"login-subtitle\">Sign in to talk with your HR team</div>\n        <div class=\"login-subtitle-ar\">سجّل دخولك للتحدث مع فريق الموارد البشرية</div>\n\n        <form id=\"loginForm\" onsubmit=\"handleLogin(event)\">\n            <div class=\"login-field\">\n                <label for=\"loginEmpNumber\">Employee Number / رقم الموظف</label>\n                <input type=\"text\" id=\"loginEmpNumber\" placeholder=\"e.g., EMP001\" required autocomplete=\"username\">\n            </div>\n            <div class=\"login-field\">\n                <label for=\"loginNationalId4\">Last 4 digits of National ID / آخر 4 أرقام من الهوية</label>\n                <input type=\"password\" id=\"loginNationalId4\" placeholder=\"1234\" required\n                       pattern=\"\\d{4}\" maxlength=\"4\" inputmode=\"numeric\" autocomplete=\"off\">\n            </div>\n            <div id=\"loginError\" class=\"login-error hidden\"></div>\n            <button type=\"submit\" class=\"login-btn\" id=\"loginBtn\">\n                Sign In / تسجيل دخول\n            </button>\n        </form>\n    </div>\n</div>\n```\n\n**Add login CSS** (inside `<style>`):\n\n```css\n.login-overlay {\n    position: fixed;\n    inset: 0;\n    background: rgba(0,0,0,0.5);\n    backdrop-filter: blur(8px);\n    display: flex;\n    align-items: center;\n    justify-content: center;\n    z-index: 1000;\n}\n.login-overlay.hidden { display: none; }\n.login-card {\n    background: var(--surface);\n    border: 1px solid var(--border);\n    border-radius: 16px;\n    padding: 40px;\n    width: 380px;\n    max-width: 90vw;\n    box-shadow: var(--shadow-lg);\n    text-align: center;\n}\n.login-logo { font-size: 28px; font-weight: 700; color: var(--accent); margin-bottom: 8px; }\n.login-subtitle { font-size: 14px; color: var(--text2); margin-bottom: 4px; }\n.login-subtitle-ar { font-size: 13px; color: var(--text3); margin-bottom: 24px; direction: rtl; }\n.login-field { text-align: left; margin-bottom: 16px; }\n.login-field label { display: block; font-size: 12px; font-weight: 500; color: var(--text2); margin-bottom: 6px; }\n.login-field input {\n    width: 100%;\n    padding: 10px 14px;\n    border: 1px solid var(--border);\n    border-radius: var(--radius-sm);\n    font-size: 14px;\n    font-family: inherit;\n    background: var(--surface2);\n    color: var(--text);\n    transition: border-color 0.15s;\n}\n.login-field input:focus { outline: none; border-color: var(--accent); }\n.login-error { color: var(--red); font-size: 13px; margin-bottom: 12px; text-align: center; }\n.login-btn {\n    width: 100%;\n    padding: 12px;\n    background: var(--accent);\n    color: white;\n    border: none;\n    border-radius: var(--radius-sm);\n    font-size: 14px;\n    font-weight: 600;\n    font-family: inherit;\n    cursor: pointer;\n    transition: background 0.15s;\n}\n.login-btn:hover { background: var(--accent2); }\n.login-btn:disabled { opacity: 0.6; cursor: not-allowed; }\n```\n\n**JavaScript changes** (in the `<script>` block):\n\n```javascript\n// ─── AUTH STATE ───\nlet chatToken = sessionStorage.getItem('krew_chat_token');\nlet chatEmployeeId = sessionStorage.getItem('krew_employee_id');\nlet chatEmployeeName = sessionStorage.getItem('krew_employee_name');\n\n// ─── LOGIN ───\nasync function handleLogin(event) {\n    event.preventDefault();\n    const empNumber = document.getElementById('loginEmpNumber').value.trim();\n    const nid4 = document.getElementById('loginNationalId4').value.trim();\n    const errorEl = document.getElementById('loginError');\n    const btn = document.getElementById('loginBtn');\n\n    errorEl.classList.add('hidden');\n    btn.disabled = true;\n    btn.textContent = 'Signing in...';\n\n    try {\n        const res = await fetch(`${API}/auth/login`, {\n            method: 'POST',\n            headers: { 'Content-Type': 'application/json' },\n            body: JSON.stringify({\n                employee_number: empNumber,\n                national_id_last4: nid4,\n            }),\n        });\n\n        if (!res.ok) {\n            const data = await res.json();\n            errorEl.textContent = data.detail || 'Login failed';\n            errorEl.classList.remove('hidden');\n            return;\n        }\n\n        const data = await res.json();\n        chatToken = data.token;\n        chatEmployeeId = data.employee_id;\n        chatEmployeeName = data.employee_name;\n\n        sessionStorage.setItem('krew_chat_token', chatToken);\n        sessionStorage.setItem('krew_employee_id', chatEmployeeId);\n        sessionStorage.setItem('krew_employee_name', chatEmployeeName);\n\n        // Set the selected employee and language from login response\n        selectedEmployee = data.employee_id;\n        selectedLanguage = data.language || 'ar';\n\n        document.getElementById('loginOverlay').classList.add('hidden');\n        onAuthSuccess();\n    } catch (err) {\n        errorEl.textContent = 'Network error. Please try again.';\n        errorEl.classList.remove('hidden');\n    } finally {\n        btn.disabled = false;\n        btn.textContent = 'Sign In / تسجيل دخول';\n    }\n}\n\nfunction onAuthSuccess() {\n    // Show the main chat interface\n    document.querySelector('.sidebar').style.display = '';\n    document.querySelector('.main').style.display = '';\n\n    // Update sidebar with employee info\n    const nameEl = document.querySelector('.sidebar-section-label');\n    if (nameEl) {\n        nameEl.textContent = `Logged in as ${chatEmployeeName}`;\n    }\n}\n\nfunction handleLogout() {\n    sessionStorage.removeItem('krew_chat_token');\n    sessionStorage.removeItem('krew_employee_id');\n    sessionStorage.removeItem('krew_employee_name');\n    chatToken = null;\n    chatEmployeeId = null;\n    chatEmployeeName = null;\n    selectedEmployee = null;\n    document.getElementById('loginOverlay').classList.remove('hidden');\n}\n\n// ─── MODIFY sendMessage to include auth header ───\n// In the existing sendMessage function, change the fetch call:\nasync function sendMessage() {\n    const input = document.getElementById('messageInput');\n    const message = input.value.trim();\n    if (!message || !chatToken) return;\n\n    // ... (existing message rendering code stays the same) ...\n\n    const res = await fetch(`${API}`, {\n        method: 'POST',\n        headers: {\n            'Content-Type': 'application/json',\n            'Authorization': `Bearer ${chatToken}`,\n        },\n        body: JSON.stringify({\n            employee_id: chatEmployeeId, // still sent for backwards compat, but server ignores it\n            message,\n        }),\n    });\n\n    if (res.status === 401) {\n        handleLogout();\n        return;\n    }\n\n    // ... (rest of existing response handling) ...\n}\n\n// ─── MODIFY resetConversation to include auth header ───\nasync function resetConversation() {\n    if (!chatToken || !chatEmployeeId) return;\n    const res = await fetch(`${API}/reset/${chatEmployeeId}`, {\n        method: 'POST',\n        headers: { 'Authorization': `Bearer ${chatToken}` },\n    });\n    if (res.status === 401) {\n        handleLogout();\n        return;\n    }\n    // ... (rest of existing reset code) ...\n}\n\n// ─── PAGE LOAD: Check for existing session ───\nwindow.addEventListener('DOMContentLoaded', async () => {\n    if (chatToken) {\n        // Verify the token is still valid\n        try {\n            const res = await fetch(`${API}/auth/verify`, {\n                method: 'POST',\n                headers: { 'Content-Type': 'application/json' },\n                body: JSON.stringify({ token: chatToken }),\n            });\n            const data = await res.json();\n            if (data.valid) {\n                selectedEmployee = chatEmployeeId;\n                document.getElementById('loginOverlay').classList.add('hidden');\n                onAuthSuccess();\n                return;\n            }\n        } catch (e) {\n            // Token invalid, fall through to login\n        }\n        handleLogout();\n    }\n    // Show login overlay (default state)\n    document.getElementById('loginOverlay').classList.remove('hidden');\n});\n```\n\n### 8. API Endpoint Summary\n\n| Method | Path | Auth | Request | Response | Description |\n|--------|------|------|---------|----------|-------------|\n| POST | `/api/v1/chat/auth/login` | None | `{employee_number, national_id_last4, tenant_id?}` | `{token, employee_id, employee_name, ...}` | Employee login |\n| POST | `/api/v1/chat/auth/verify` | None | `{token}` | `{valid, employee_id, employee_name}` | Verify token validity |\n| POST | `/api/v1/chat/auth/logout` | None | - | `{status}` | Logout (client-side) |\n| POST | `/api/v1/chat` | Bearer (chat) | `{message}` | `{agent, response, conversation_id}` | Send chat message (AUTHENTICATED) |\n| POST | `/api/v1/chat/reset/{employee_id}` | Bearer (chat) | - | `{status}` | Reset conversation (AUTHENTICATED) |\n| GET | `/api/v1/chat/employees` | None | `?tenant_id=` | `[{id, name, ...}]` | List employees (unchanged, no sensitive data) |\n| GET | `/api/v1/chat/tenants` | None | - | `[{id, name, name_ar}]` | List tenants (unchanged) |\n\n### No migrations needed for A2\n\nUses existing tables and stateless JWT tokens.\n\n---\n\n## A3: Conversation Quality Improvements\n\n### 3a. Conversation Memory\n\nThe current implementation already loads the last 20 messages from the database and sends them as conversation history to Claude (see `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/api/chat.py` lines 102-115). This is correct. However, there are two improvements:\n\n1. **System messages are being sent as `assistant` role**, which confuses Claude. System messages (escalation notices) should be filtered out.\n2. **The message limit of 20 is good**, but the orchestrator appends the new user message again (line 117 of orchestrator.py), creating a duplicate. The fix is to not append in the orchestrator when history is already provided.\n\n**Changes to `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/api/chat.py`** (history loading section):\n\n```python\n    # Load conversation history — newest 20 messages, then reverse to chronological\n    msg_result = await db.execute(\n        select(Message)\n        .where(\n            Message.conversation_id == conversation.id,\n            Message.role != \"system\",  # FIX: exclude system messages from history\n        )\n        .order_by(Message.created_at.desc())\n        .limit(20)\n    )\n    history = [\n        {\n            \"role\": \"user\" if m.role == \"employee\" else \"assistant\",\n            \"content\": m.content,\n        }\n        for m in reversed(msg_result.scalars().all())\n    ]\n```\n\n### 3b. Better Error Messages\n\n**Changes to `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/agents/base.py`** (in the `respond` method, around the tool call handling):\n\n```python\n            # Handle tool calls\n            if response.stop_reason == \"tool_use\":\n                current_messages.append({\"role\": \"assistant\", \"content\": response.content})\n\n                tool_results = []\n                for block in response.content:\n                    if block.type == \"tool_use\":\n                        try:\n                            result = await self.handle_tool_call(block.name, block.input)\n                        except Exception as e:\n                            logger.error(\n                                \"Tool %s failed for employee %s: %s\",\n                                block.name, employee_id, e,\n                            )\n                            result = json.dumps({\n                                \"error\": \"An internal error occurred while processing your request. Please try again.\",\n                                \"error_ar\": \"حدث خطأ داخلي أثناء معالجة طلبك. يرجى المحاولة مرة أخرى.\",\n                                \"tool\": block.name,\n                            })\n                        tool_results.append({\n                            \"type\": \"tool_result\",\n                            \"tool_use_id\": block.id,\n                            \"content\": result,\n                        })\n\n                current_messages.append({\"role\": \"user\", \"content\": tool_results})\n```\n\nNote: The `import json` is already present in `base.py` -- it just needs to be verified. Actually, checking the current base.py, `json` is already imported on line 2.\n\n### 3c. Greeting Personalization with Cultural Awareness\n\n**Changes to `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/agents/base.py`** (in `get_system_prompt`):\n\nAdd Ramadan and cultural awareness after the holiday section. Insert the following block after the `holiday_note` line (around line 72) and before the final return:\n\n```python\n        # Cultural event awareness\n        from app.saudi_holidays import _EID_DATES\n        cultural_notes = []\n\n        # Ramadan awareness — approximate: Eid Al-Fitr dates minus 30 days\n        year_eids = _EID_DATES.get(today.year, {})\n        eid_fitr_dates = year_eids.get(\"eid_fitr\", [])\n        if eid_fitr_dates:\n            ramadan_approx_start = eid_fitr_dates[0] - timedelta(days=30)\n            ramadan_approx_end = eid_fitr_dates[0] - timedelta(days=1)\n            if ramadan_approx_start <= today <= ramadan_approx_end:\n                cultural_notes.append(\n                    \"It is currently Ramadan. Greet with 'رمضان كريم / Ramadan Kareem'. \"\n                    \"Be mindful that working hours are reduced (6 hours/day). \"\n                    \"Avoid scheduling meetings during iftar time.\"\n                )\n            elif today in eid_fitr_dates:\n                cultural_notes.append(\n                    \"Today is Eid Al-Fitr! Greet with 'عيد فطر مبارك / Eid Mubarak'. \"\n                    \"This is a public holiday.\"\n                )\n\n        eid_adha_dates = year_eids.get(\"eid_adha\", [])\n        if eid_adha_dates and today in eid_adha_dates:\n            cultural_notes.append(\n                \"Today is Eid Al-Adha! Greet with 'عيد أضحى مبارك / Eid Adha Mubarak'. \"\n                \"This is a public holiday.\"\n            )\n\n        # Founding Day\n        if today.month == 2 and today.day == 22:\n            cultural_notes.append(\n                \"Today is Saudi Founding Day (يوم التأسيس)! Mention it warmly in your greeting.\"\n            )\n\n        # National Day\n        if today.month == 9 and today.day == 23:\n            cultural_notes.append(\n                \"Today is Saudi National Day (اليوم الوطني)! Celebrate with a patriotic greeting.\"\n            )\n\n        cultural_context = \"\"\n        if cultural_notes:\n            cultural_context = \"\\n\\nCultural context for today:\\n\" + \"\\n\".join(f\"- {n}\" for n in cultural_notes)\n```\n\nThen in the return statement, add `{cultural_context}` after the holiday info block and before `Rules:`:\n\n```python\n        return f\"\"\"You are {self.name} ({self.name_ar}), a virtual HR employee at Krew.\n\nRole: {self.role}\nDivision: {self.division}\nPersonality: {self.personality}\n\nToday is {today.isoformat()} ({day_name}). Use this to resolve relative dates like \"next Sunday\" or \"this week\".{holiday_note}\nYou are currently helping {employee_name}. Their employee_id is {employee_id}.\n{lang_instruction}\n\nSaudi public holidays for {today.year} (these are NOT deducted from employee leave balance):\n{holiday_info}\nWhen an employee's leave request spans a public holiday, mention that the holiday is not counted as a leave day.\nWhen discussing the team calendar, mention any upcoming public holidays in the period.\n{cultural_context}\n\nGreeting instructions:\n- When this is the first message in a conversation, greet {employee_name} warmly by name.\n- Use time-appropriate greetings: \"صباح الخير\" (morning), \"مساء الخير\" (afternoon/evening).\n- If today is during Ramadan, include \"رمضان كريم\".\n- If today is an Eid, include the appropriate Eid greeting.\n- If there is proactive context (pending requests, open tickets), mention it naturally in the greeting.\n\nRules:\n...\n\"\"\"\n```\n\nNote: `timedelta` must be imported at the top of `get_system_prompt`. It is already available since `datetime` is imported from the standard library. Add `from datetime import date as _date, timedelta as _timedelta` or use the existing import pattern in the method.\n\n### 3d. Complete updated get_system_prompt\n\nHere is the complete updated method to replace the existing one in `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/agents/base.py`:\n\n```python\n    def get_system_prompt(self, employee_name: str, employee_id: str, language: str = \"ar\") -> str:\n        lang_instruction = (\n            \"Respond in Arabic. Use casual Saudi dialect when appropriate.\"\n            if language == \"ar\"\n            else \"Respond in English.\"\n        )\n\n        from datetime import date as _date, timedelta as _timedelta\n        from app.saudi_holidays import format_holidays_for_prompt, get_holiday_name, _EID_DATES\n\n        today = _date.today()\n        weekdays_ar = [\"الاثنين\", \"الثلاثاء\", \"الأربعاء\", \"الخميس\", \"الجمعة\", \"السبت\", \"الأحد\"]\n        weekdays_en = [\"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\", \"Saturday\", \"Sunday\"]\n        day_name = weekdays_ar[today.weekday()] if language == \"ar\" else weekdays_en[today.weekday()]\n\n        # Holiday awareness\n        holiday_info = format_holidays_for_prompt(today.year)\n        today_holiday = get_holiday_name(today)\n        holiday_note = f\"\\nToday is a public holiday: {today_holiday}.\" if today_holiday else \"\"\n\n        # Cultural event awareness\n        cultural_notes = []\n        year_eids = _EID_DATES.get(today.year, {})\n        eid_fitr_dates = year_eids.get(\"eid_fitr\", [])\n        if eid_fitr_dates:\n            ramadan_approx_start = eid_fitr_dates[0] - _timedelta(days=30)\n            ramadan_approx_end = eid_fitr_dates[0] - _timedelta(days=1)\n            if ramadan_approx_start <= today <= ramadan_approx_end:\n                cultural_notes.append(\n                    \"It is currently Ramadan. Greet with 'رمضان كريم / Ramadan Kareem'. \"\n                    \"Be mindful that working hours are reduced (6 hours/day). \"\n                    \"Avoid scheduling meetings during iftar time.\"\n                )\n            elif today in eid_fitr_dates:\n                cultural_notes.append(\n                    \"Today is Eid Al-Fitr! Greet with 'عيد فطر مبارك / Eid Mubarak'. \"\n                    \"This is a public holiday.\"\n                )\n\n        eid_adha_dates = year_eids.get(\"eid_adha\", [])\n        if eid_adha_dates and today in eid_adha_dates:\n            cultural_notes.append(\n                \"Today is Eid Al-Adha! Greet with 'عيد أضحى مبارك / Eid Adha Mubarak'. \"\n                \"This is a public holiday.\"\n            )\n\n        if today.month == 2 and today.day == 22:\n            cultural_notes.append(\n                \"Today is Saudi Founding Day (يوم التأسيس)! Mention it warmly in your greeting.\"\n            )\n        if today.month == 9 and today.day == 23:\n            cultural_notes.append(\n                \"Today is Saudi National Day (اليوم الوطني)! Celebrate with a patriotic greeting.\"\n            )\n\n        cultural_context = \"\"\n        if cultural_notes:\n            cultural_context = \"\\n\\nCultural context for today:\\n\" + \"\\n\".join(f\"- {n}\" for n in cultural_notes)\n\n        return f\"\"\"You are {self.name} ({self.name_ar}), a virtual HR employee at Krew.\n\nRole: {self.role}\nDivision: {self.division}\nPersonality: {self.personality}\n\nToday is {today.isoformat()} ({day_name}). Use this to resolve relative dates like \"next Sunday\" or \"this week\".{holiday_note}\nYou are currently helping {employee_name}. Their employee_id is {employee_id}.\n{lang_instruction}\n\nSaudi public holidays for {today.year} (these are NOT deducted from employee leave balance):\n{holiday_info}\nWhen an employee's leave request spans a public holiday, mention that the holiday is not counted as a leave day.\nWhen discussing the team calendar, mention any upcoming public holidays in the period.\n{cultural_context}\n\nGreeting instructions:\n- When this is the FIRST message in a conversation, greet {employee_name} warmly by name.\n- Use time-appropriate greetings: \"صباح الخير\" (morning before noon), \"مساء الخير\" (afternoon/evening).\n- If today is during Ramadan, include \"رمضان كريم\".\n- If today is an Eid, include the appropriate Eid greeting.\n- If there is proactive context (pending requests, open tickets), weave it naturally into the greeting.\n- For subsequent messages in the conversation, do NOT repeat the greeting.\n\nRules:\n- You are a colleague, not a chatbot. Be warm, professional, and helpful.\n- Always use the employee's name.\n- When calling tools that need employee_id, use: {employee_id}\n- If you need data, use your tools. Never guess or make up information.\n- If you cannot resolve something, explain why and offer to escalate to a human.\n- For leave requests, always check the balance first.\n- For policy questions, always search the company policies first.\n- Keep responses concise — this is a chat, not an email.\n- When a tool returns an error, explain it kindly in the user's language. Never show raw JSON errors.\n\nLeave-type-specific rules:\n- Maternity leave: Only available to female employees (gated by balance existence). 70 days entitlement. Use a warm, congratulatory tone (e.g., \"مبروك! / Congratulations!\").\n- Paternity leave: Only available to male employees (gated by balance existence). 3 days entitlement.\n- Bereavement leave: Use a compassionate, condolence tone. Say \"الله يرحمه/يرحمها\" (May God have mercy on them) or \"عظم الله أجركم\" when appropriate.\n- Unpaid leave: Always ask the employee for explicit confirmation before submitting, since unpaid leave affects salary. Explain the impact clearly.\n- Hajj leave: One-time entitlement during employment. Use a respectful, reverent tone (e.g., \"حج مبرور إن شاء الله / May your Hajj be accepted\").\n- Emergency leave: May be deducted from annual leave balance if emergency balance is exhausted. Inform the employee of this possibility.\n- If an employee requests a leave type they don't have a balance for, it means they are not eligible. Do not suggest creating a balance — that is an admin task.\n\nEscalation rules:\n- Use the escalate_to_human tool when:\n  1. The employee explicitly asks to speak with a human / مسؤول / مدير\n  2. You cannot resolve the issue after reasonable attempts\n  3. The topic is sensitive (salary disputes, harassment, termination, legal matters)\n  4. There is a policy gap — the employee is asking about something not covered by existing policies\n- When escalating, always inform the employee that a human will follow up, and provide the ticket ID.\n- After escalating, do not continue trying to resolve the issue yourself.\n\"\"\"\n```\n\n---\n\n## Database Migration Notes\n\n**No new migration is needed for Sprint 3 Batch A.** All three features use existing tables:\n- A1 uses `hr_policies`, `policies`, `policy_chunks` (all existing)\n- A2 uses `employees` table for auth lookup + stateless JWT tokens (no new tables)\n- A3 is code-only changes to agent prompts and error handling\n\n---\n\n## Integration Points\n\n```\nA1: Seed Policies\n┌─────────────┐     ┌──────────────┐     ┌───────────────┐\n│ seed script  │────>│  hr_policies │     │   policies    │\n│              │     │  (CRUD table)│     │ (RAG parent)  │\n│              │────>│              │────>│               │\n│              │     └──────────────┘     │ policy_chunks │\n│              │                          │ (embeddings)  │\n└─────────────┘                          └───────────────┘\n                                               │\n                                               v\n                                      PolicyRetriever.search()\n                                               │\n                                               v\n                                         Deema agent\n\nA2: Chat Auth\n┌───────────┐     ┌─────────────────┐     ┌──────────────┐\n│ chat.html │────>│ /chat/auth/login│────>│  employees   │\n│ (login    │     │ (verify emp#    │     │  (national_id│\n│  form)    │     │  + NID last 4)  │     │  lookup)     │\n│           │     └────────┬────────┘     └──────────────┘\n│           │              │\n│           │         JWT token\n│           │     (type=employee_chat)\n│           │              │\n│           │     ┌────────v────────┐\n│           │────>│  POST /chat     │ <── Authorization: Bearer <token>\n│           │     │  (authenticated)│\n│           │     └─────────────────┘\n└───────────┘\n\nA3: Quality\n┌────────────┐     ┌──────────────────┐\n│ base.py    │────>│ Cultural context  │\n│ (system    │     │ (Ramadan, Eid,    │\n│  prompt)   │     │  holidays)        │\n│            │     └──────────────────┘\n│            │     ┌──────────────────┐\n│            │────>│ Error wrapping    │\n│            │     │ (try/catch tools) │\n│            │     └──────────────────┘\n└────────────┘\n```\n\n---\n\n## File Summary\n\nFiles to CREATE:\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/scripts/seed_policies.py` -- A1 seed script\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/api/chat_auth.py` -- A2 login/verify/logout endpoints\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/auth/chat_dependencies.py` -- A2 chat auth dependency\n\nFiles to MODIFY:\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/auth/jwt.py` -- A2 add `create_chat_token` function\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/api/chat.py` -- A2 add auth to POST/reset endpoints; A3 fix history filtering\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/main.py` -- A2 register `chat_auth` router\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/agents/base.py` -- A3 updated `get_system_prompt` + tool error wrapping in `respond`\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/static/chat.html` -- A2 login modal + auth headers\n\nFiles NOT modified (confirmed safe):\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/auth/dependencies.py` -- admin auth unchanged\n- `/Users/najwamalghamdi/Desktop/HR-AI-Startup/backend/app/agents/deema.py` -- no changes needed (quality improvements are in base.py)\n\n---\n\n## Open Questions for CTO\n\n1. **A2 login credentials**: I chose `employee_number` + last 4 digits of `national_id` as the authentication factor. An alternative is `email` + PIN. The national_id approach is more Saudi-native and does not require setting up PINs. However, it requires that `national_id` is populated for all employees. Should we fall back to email-based verification if national_id is null?\n\n2. **A2 token expiry**: I set 8 hours (one work day). Should this be shorter (4 hours) or longer (24 hours)? Shorter is more secure but more friction for employees who leave the tab open.\n\n3. **A2 employee dropdown visibility**: Currently `/chat/employees` returns employee names and job titles without auth. This is needed for the tenant selector before login. The data exposed is non-sensitive (name, job title, employee number). Should we restrict this further, for example requiring the login to happen first and removing the employee dropdown entirely (since the token now identifies the employee)?\n\n4. **A1 policy count**: I have designed 12 comprehensive bilingual policies covering all the question categories listed. Should we add more (e.g., separate travel policy, Saudization/Nitaqat policy, intellectual property policy)?\n\n5. **A3 Ramadan detection**: Ramadan start dates are approximated by subtracting 30 days from Eid Al-Fitr. For production accuracy, should we add explicit Ramadan start dates to `_EID_DATES` in `saudi_holidays.py`?"}],"stop_reason":"end_turn","stop_sequence":null,"usage":{"input_tokens":1,"cache_creation_input_tokens":762,"cache_read_input_tokens":69192,"output_tokens":33352,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":762},"inference_geo":"","iterations":[],"speed":"standard"}},"requestId":"req_011CZL7Xusrj2tpyQfmuHWCo","type":"assistant","uuid":"a6896509-d363-452f-b4dd-5a57368e6de4","timestamp":"2026-03-23T13:53:35.519Z","userType":"external","cwd":"/Users/najwamalghamdi/Desktop/HR-AI-Startup","sessionId":"0562ac40-6f53-42b4-9739-c109edcd4538","version":"2.1.77","gitBranch":"HEAD","slug":"floating-dancing-steele"}
